# moire-eye-1 — Session Journal

## Knob map (current on moire-eye-2, the live canvas from iter 21)
- knob_1 ZOOM (FOV 0.45..1.30) · knob_2 iris hue tint · knob_6 gold-core strength · knob_7 facet/fiber density · knob_9 gaze Y · knob_11 bass reactivity · knob_12 gaze X · knob_14 pupil size · knob_15 airglow intensity · knob_16 sparkle/ember · knob_19 catchlight+gaze strength. Unwired/free: 5,8,10,13 (candidates: spin speed / arm spread / twist / ripple).
- Iter 21: controller took moire-eye-2 live; wired knob_7/11/15. Run now follows the display onto moire-eye-2.
- Iter 30 — FIXED FLATNESS (user: "flatness is not solved; use imagemagick or something so you can SEE when color is flat"). Root cause: hue varied (radial gradient) but the SURFACE was flat — no luminance texture (it was an airbrushed gradient orb). Added radial ciliary FIBRE striations (`0.5+0.5*sin(ang*fiberN + sin(ang*fiberN*0.5)*1.5)`, fiberN=50+knob_7*90, gated to stroma band), CRYPTS (angular pits), and SPHERICAL shading (curvature darkening toward limbal) — all static (no flash). Result reads as a real dimensional iris. OBJECTIVE: built an in-page canvas pixel meter (ImageMagick NOT installed — convert=Windows trap). localDetail 6.9→46.4 (soft→TEXTURED), Lstd 24%→55%. LESSON: "flat" = low LUMINANCE/surface texture, not low hue variation — measure localDetail, and add fibre/shading detail, not more hues.
- Iter 27-28 — PEACH-PLASMA PALETTE + slower spin (user: "colour is flat; spinning too fast; use plasma/peach journal"). Replaced the flat single-hue with a RADIAL gradient from magic-peach: `hue = mix(0.6, 4.2, smoothstep(0,0.52,er)) + (knob_2-0.5)*3 + hue_phase*0.15 + warmth*0.3` — hot orange-gold CORE -> blue-violet CORONA = emotional depth, smooth (NOT banded). `satBoost = 0.75 + max(energyZScore,0)*0.45` (peach SAT_BOOST, energy pumps saturation, z-score accents only). Iter 27 also de-stippled (`O(nt,1,.72)`). Controller spin/flow base rates cut again (~half). NOTE: knob_47 spin + knob_4 flow both = 0 (frozen) but base moiré still evolves via global `t=iTime/60` (nt=n*t) + camera dolly O(t/2,80,110) — if user still says "too fast" with knobs at 0, those are the culprits to freeze next.
- Iter 26 — OKLCH COLOUR REWRITE + de-flicker round 2 (user: "spin too fast; no multicolour banding; no particles; use oklch"). Moiré now accumulates a single GRAYSCALE intensity `glow` (dropped `H()` rainbow + `2e2/n` particle term); colourised once via `oklch2rgb(L, C, hue)` with ONE hue family (knob_2 base + hue_phase*0.3 drift + core-warm shift). Overlays (collarette/airglow/outer) recoloured into the same family. Controller spin/flow base rates cut ~3x. Spin re-linked to knob_47 (user wiggled it to identify it); knob_4 flow, knob_5 hue. CRITICAL: remote controller overrides /__save-shader on the display → had to push `window.cranes.shader = <new code>` directly. Remaining: dotted stroma texture still present (see Todo).
- Iter 24: USER asked for speed dials → SPEED CONTROLS now on knob_3 spin, knob_4 flow, knob_5 hue (0=frozen, 0.5=default, 1=2x). (Superseded the iter-23 8/10/5 mapping.) IMPORTANT: controller file-save HMR did NOT auto-apply — had to force `window._hotController = (await import('/controllers/moire-eye.js?t='+Date.now())).default`. Do this re-import after every controllers/moire-eye.js edit.
- Iter 23: wired free knobs to MONOTONIC phase RATES in the controller — knob_8 spin speed, knob_10 flow speed, knob_5 hue-drift speed (superseded iter 24). Flash-free expressive motion control (on-mandate). Full wired set now: 1 zoom, 2 hue, 5 hue-speed, 6 gold, 7 facet, 8 spin-speed, 9 gazeY, 10 flow-speed, 11 bass-react, 12 gazeX, 13 drop-decay, 14 pupil, 15 airglow, 16 outer-glow, 19 catchlight. User also sweeping knob_34-53 (unwired in this eye — iris-7 leftovers on the MIDI controller; intentionally NOT wired to avoid clutter/flashing).
- Remote: run the jam display with `&remote=display` so the controller's knob twists reach it (dropped it once during a restore — re-added iter ~20).

## Status
Iter 53 — **FORKED to moire-eye-3** (user: "we can fork" + "I do not see the music reactivity" + "you need to be using the spectral features, as well as the stats on those features, as the uniforms"). eye-3 = eye-1's calmed/flicker-fixed body + a RICH spectral audio map. Live canvas is now moire-eye-3. Track: Subtronics "Hieroglyph (Of The Trees Remix)" (bass dubstep).

## Forks
- `moire-eye-5 ← moire-eye-4` (iter 65, user `/fork`): checkpoint snapshot of the full-frame look (identical code to eye-4 at fork time) — a stable keeper before further experiments. Live canvas now eye-5. Baked preset in moire-eye-5.md (knob_1=1, knob_11=0.827, knob_7=1, knob_14=0.488).
- `moire-eye-4 ← moire-eye-3` (iter 64, user `/fork` + new direction): FULL-FRAME. User: "take up the full frame now. Tendrils/arms of fractals radiate around the eye, moving in curly ways with the music. More tunneling effect into the pupil." Added (1) FRACTAL TENDRILS — the moiré `c` field beyond the iris (er>0.46) carved into spiralling radial arms, `curl=ang*armN + er*twist*TAU - iTime*speed`, armN from spread, twist from flux+energy, 2 octaves, bass-pulsed, fading to corners → fills the void; (2) PUPIL TUNNEL — `sin(1/er*5 - iTime*1.5 + ang*3)` concentric rings receding inward (1/er = perspective compression), bass deepens; (3) carried eye-3 anatomy + spectral map + TREND. Screenshot (Effin "What's In Your Heart"): full-frame composition — bright bold iris centre, clear ringed tunnel into the pupil, curling tendril field filling all corners. Tendrils subtle at REACT=0; crank knob_11 for more curl. Journal lives on moire-eye-1-cool-moments.md (shared lineage).
- `moire-eye-3 ← moire-eye-1` (iter 53): reactivity rework. eye-1 was de-twitched so hard the music went invisible; eye-3 drives a variety of independent spectral features + their stats (normalized/zScore/slope/rSquared) into distinct eye parts, blend-tamed so it breathes not strobes. knob_11 = master REACT (0.5x..2x). See `shaders/redaphid/wip/moire-3d/moire-eye-3.md` for the full map.

## Status (historical)
Iter 2 — eye fusion live. Added entropy-driven stroma shimmer. Active in the `/vibej` run. moveStyle subtle. Track rotation: Carbon Based Lifeforms / Applefish, Pulse Mandala (ambient psybient).

## Cool moments
- **Iter 66 (moire-eye-5) — FULL-FRAME EYE-PORTAL / TUNNEL + CURLY TENDRILS (GWN "Bark Mode", 2026-06-07).** Knobs: knob_1≈0.7 (zoom), knob_7=1 (dense fibres), knob_11=0.835 (REACT high), knob_14=1 (pupil full open), knob_13=1 (max smoothing). **Visual:** the realised full-frame vision — a vivid radial spoke-iris (yellow/blue/purple/green ciliary fibres) wrapping a deep red-orange CONCENTRIC TUNNEL that recedes to a vanishing point in the pupil (real depth), and the whole frame filled with bold flowing CURLY tendril ridge-fields that wave/curl outward to every corner (topographic-fabric look from the moiré cells). White catchlight. **What worked:** iter-66 tendril rework (radius-dependent `wave` bend → arms curl instead of straight-spiral; exponent 3→2 = bolder ribbons; tendZone reach extended; c*1.5→2.2 brighter) + the iter-64 pupil tunnel (`sin(1/er*5 - iTime*1.5 + ang*3)`). Reads as a hypnotic eye-portal / wormhole-iris. **Design hypothesis:** the moiré field is a natural full-frame substrate — masked to a disc it's an iris, carved into curling arms it's a tendril field, compressed by 1/er it's a tunnel; all three from one raymarch. Tendril outer zone is greyish (moiré dim far out) — inject more colour there for v(next) if wanted.
- **Iter 62 (moire-eye-3) — PHOTOREAL IRIS / DE-NOISE PAYOFF (OTT "Pressure", 2026-06-07).** **Audio fingerprint:** the deep-clean MONSTER_BASS corner — bass 0.60 + mids 0.72 + treble 0.01 + **centroid 0.05 + entropy 0.04 (ultra-clean) + crest 0.85 (peaky) + spread 0.02**. Knobs: knob_1=1 (max zoom-in), knob_2=1, knob_7≈0.9 (dense fibres), knob_11=0.91 (REACT high), knob_13=1 (max anti-flicker), knob_14≈0.25 (pupil), knob_15=1 (airglow). **Visual:** the cleanest, most realistic eye of the run — large dark central pupil + wet white catchlight upper-left; **gold-amber ciliary stroma fibres** radiating from the pupil (sharp, because crest 0.85 + iris-7's crest=sharpness); a **green wavy COLLARETTE ring** circling the pupil; **blue-violet outer stroma fibres** to the rim; red-orange bands woven through; clean dark **limbal ring** frame; and the **CRYPTS OF FUCHS** rendering as crisp distinct dark cell-pits in a right-side crescent (iris-7 lesson confirmed: clean audio entropy<0.10 → crispest crypt geometry). **What worked:** the iter-61 de-noise (dropped sin(n*40) shimmer, raised cell floor .3→.78) turned the grainy moiré body into a smooth fibre field, so the smooth anatomy (fibres/collarette/ruff/limbal) now reads on a clean substrate — moiré DEPTH kept, moiré NOISE gone. **Design hypothesis:** this deep-clean-bass corner is the shader's "real eye" hero zone; the crypts are a feature here, not noise. Candidate keeper — fork to moire-eye-4 if the user wants to lock it.
- **Iter 34 (Myhja — Lauge/Baba Gnohm) — USER APPROVED ("much better").** After the moire-eye-2 rework (peach-plasma radial gradient + screen-space fibre iris + monotonic controller) the user said "this is not great, revert to the first fork." Reverted display to **moire-eye-1** (original moiré-hex-sphere stroma + iris-7 anatomy) → user: "much better". LESSON: the moiré-hex-sphere IS the magic — its 3D tunnel rings + sphere lattice make the iris read as deep/organic/alive in a way the flat screen-space fibre rebuild (eye-2) never did. The "particles/flat" complaints about eye-2 were really about the rebuild losing the moiré depth. Keep moire-eye-1's moiré core. moire-eye-2 set aside (not deleted).
- **Iter 15 (Blue Airglow, screenshot review)** — Composition assessed as complete/balanced: gold flame-petal stroma + red ruff + blue accents, clean black pupil, dark limbal frame, cyan airglow rim, shimmering starburst filigree. **Restraint call: no edit** — audio silent + piece is dialed in; further stacking would blow it out. Design hypothesis: once an eye reaches ~6-8 well-separated couplings + full anatomy, switch from adding to occasional refinement only.
- **Iter 1 (Cosmic Alignment — Applefish)** — Audio fingerprint: bass swell (bassNormalized high). What worked: the bass z-pulse amplitude (`sin(nt)*(1+bassNormalized*0.6)`) pulled the moiré spheres into a bold radial bloom — red/gold flame-petals + teal cellular stroma around a wide black pupil, gold limbal ring, white catchlight upper-left, dense filigree fading to black void. The moiré hex-sphere field maps PERFECTLY onto iris fibers; the tunnel's concentric rings become anatomical bands automatically. Design hypothesis: the radial moiré tunnel is the ideal substrate for eye anatomy — no separate fiber synthesis needed.

## Todo
- `[x] smooth the dotted/particle stroma texture` — iter 27: softened per-cell moiré contrast `O(nt,1,.3)` → `O(nt,1,.72)`. Stippling essentially gone — stroma now smooth (verified by screenshot: clean orange iris). Faint residual stipple only at extreme bottom-left rim; revisit if user flags.
- `[ ] confirm spin feels right` — knob_47 now spin speed; user said "too fast", base rate cut ~3x + knob can freeze. Verify with user.
- `[x] gaze life` — iter 3: added micro-saccade (autonomous gaze drift via incommensurate sines, amp 0.012). Eye drifts gently even when fixated.
- `[x] treble sparkle` — iter 5: twinkling cool-white glints on outer stroma, gated by trebleNormalized + outer band. Stars on airy passages.
- `[x] limbal ring contrast` — iter 7: deepened + repositioned the limbal to a dark band at the outer edge (peak er 0.48, darken *0.85, irisMask widened to 0.56→0.46) so the eye is framed.
- `[x] pupil-tunnel depth` — iter 8: `maw = c*vec3(0.55,0.12,0.06)*0.13; col = mix(maw, col, pupil)`. Faint dark-red moiré tunnel now bleeds into the pupil centre — ribbed arcs spiral into the maw instead of flat black. Done during a silent tick (audio-independent structural refinement).

## History of changes
- Iter 61 (moire-eye-3) — THREE moves: (1) FURROW STROBE FIX: the contraction furrows had `bassNormalized*4*REACT` INSIDE the sin phase → at REACT=2 that's ~8 rad of phase jump per kick = strobe (the plasma "audio-in-phase=strobe" trap, which I'd reintroduced). Fixed: furrows drift on `iTime*0.6` (smooth) and bass DEEPENS them via amplitude (`furrowDepth = furrowBand*(0.4+bass*0.5*REACT)`). (2) TREND / "Rslopes" (user: "focus on the Rslopes, I want to see what that looks like"): added `TREND = clamp((spectralCentroidSlope*RSquared + energySlope*RSquared)*55, -1, 1) * (0.4+knob_8*1.6)` — slope×rSquared = direction-of-music × confidence; + = steady build, − = confident drop, chaotic≈0. Wired to PUPIL dilation (`+TREND*0.6`: dilates through builds, constricts on the drop) + airglow rim radius/brightness. knob_8 = TREND amount (crank to see it). (3) DE-NOISE the moiré (user: "remove the moire noise"): the grainy speckle in the iris body was the moiré `c` accumulation — dropped the hi-freq `entropy*sin(n*40)` angular shimmer (literal noise), raised cell-contrast floor `O(nt,1,.3)→.78` (the iter-27 de-stipple), tamed spike `2e2/n → 1.2e2/n`. Screenshot (OTT "Pressure", knob_13=1 max smoothing): smooth vivid tie-dye iris, gold radial fibres, blue/red bands, clean pupil, limbal ring — moiré speckle gone from the body. Residual dotted texture only at the lower-right rim (crypts + edge cells) — thin those if user still flags. LESSON: "moire noise" = the per-cell speckle (hi-freq sin + sharp cell contrast + `2e2/n` spikes), NOT the moiré tunnel structure (which the user approved iter34). De-noise by raising the cell floor + dropping hi-freq terms, keep the tunnel rings.
- Iter 59 (moire-eye-3, screenshot-caught bug): GAZE DECOUPLING fix. At strong gaze (knob_9/12 near extremes + zoomed out) the eye SPLIT — the moiré iris body and the screen-space anatomy (pupil/collarette/ruff/fibres) flew apart in opposite directions, body sinking while the rings rose. Root cause: the moiré tunnel ROTATES with gaze (`p.xz*=A(m.x)`) while the anatomy TRANSLATES with gaze (`ec -= gaze*0.5`) — at large gaze these move opposite ways and the two layers decouple. Fix: GAZE_STR `0.35+k19*0.65 → 0.18+k19*0.30`, clamp gaze to ±0.34, moiré rotation `gaze*vec2(0.6,0.5) → vec2(0.45,0.4)`, anatomy translation `gaze*0.5 → gaze*0.30`. Now both layers turn together by a small coherent amount; the eye glances without coming apart. Screenshot verified: at knob_9=1 the iris stays a unified fiery volcanic eye (dark pupil, tight gold collarette ring, dense red-orange radial fibres, catchlight). LESSON: when two coordinate systems represent the same object (a rotated 3D body + a translated 2D overlay), gaze/parallax must move them by MATCHED visual amounts or they decouple at the extremes — keep the gaze gentle+clamped, or drive both from one transform. Same class as the plasma "SDF wander carves chunks" bug. Screenshot before claiming a gaze/compose fix.
- Iter 56 (moire-eye-3, user: "brightness is lazy, try harder — read the iris journals and peach/plasma" + "we had concepts of the structures of irises"). RIPPED OUT the brightness pumping; rebuilt reactivity on the magic-peach/iris-7 philosophy: **drive COLOUR, GEOMETRY, SHARPNESS — never luminance.** Changes: (1) removed `col *= 1+energy` brightness breath + buildN brightness; (2) ENERGY → SATURATION (peach SAT_BOOST: `mix(vec3(luma), col, 0.55+energyN*0.55+max(energyZ,0)*0.4)` — loud=vivid, quiet=grey); (3) CENTROID → HUE ROTATION via rgb2hsl (bright songs sit cooler) + pitchClass accent; (4) CREST → SHARPNESS of stroma striae + ruff scallops (`pow(streak, mix(1,3.5,crest))` — iris-7's "crest is the sharpness modulator"); (5) brought back the IRIS STRUCTURES: CRYPTS OF FUCHS (angular pits, entropy blinks them open — iris-7 crypt-blink), CONTRACTION FURROWS (concentric ridges that tighten inward on bass = musculature contracting, GEOMETRY not glow), PUPILLARY RUFF (iris-7's "key eye cue" — bright wavy gold scalloped ring at the pupil edge, crest sharpens scallops, treble glints). Bass stays on MASS (tunnel z-swell + pupil dilation). Screenshot on OTT "Dream Atlas": real structured iris — gold ribbed star pupil, bold wavy collarette ring, red/amber radial fibres, crypt-pit scatter, blue airglow halo, catchlight. LESSON: brightness modulation is the lazy reflex; the rich move is colour (sat+hue) + anatomical structure (crypts/furrows/ruff) + sharpness, each on an independent spectral feature. rgb2hsl/hsl2rgb ARE available in the wrapper.
- Iter 53 (FORK → moire-eye-3, user: "I do not see the music reactivity" + "use the spectral features AND the stats on those features as the uniforms"). The de-twitch (iter 39) + frame-blend (iter 51) had shrunk every audio coefficient to near-zero, so the eye stopped responding visibly. eye-3 keeps the blend (the flicker cure) but drives a VARIETY of independent spectral features + stats, each into a distinct eye part: pupil ← bass+energyZScore+spectralFluxZScore; iris brightness ← energyNormalized + spectralCentroidSlope×RSquared (confident build); tunnel ripple rate ← spectralFluxNormalized; arms width ← spectralSpreadNormalized; stroma grit ← spectralRoughnessNormalized; warmth ← centroid+spectralSkew; rim radius ← spectralRolloffNormalized; catchlight sharpness ← spectralCrestNormalized; vignette focus ← spectralKurtosisNormalized; shimmer ← spectralEntropyNormalized; collarette ← pitchClass. knob_11 = master REACT (0.5x..2x). KEY: the frame-blend low-passes the reactions, so even punchy zScore/flux terms read as breathing — this is what lets us use zScore/transient stats WITHOUT the iter-39 twitch. LESSON: don't fix twitch by shrinking reactions to nothing; fix it with the temporal blend, then drive BIG reactions through it.
- Iter 51 (on moire-eye-1, user: "the flickering is driving me crazy" — I'd been coasting on holds, BAD). ROOT CAUSE finally diagnosed: the flicker is the per-frame MOIRÉ RAYMARCH itself — fine sphere-lattice detail scintillates every frame as `t=iTime/60` advances `nt=n*t`. Trimming individual effects never fixed it. REAL FIX = TEMPORAL SMOOTHING: `vec3 prevF=getLastFrameColor(U/R).rgb; outc=mix(prevF, outc, fb)` where `fb=mix(0.55,0.18,knob_13)` (knob_13 high = heavier persistence = smoother). Kills per-frame scintillation by low-passing output over time. LESSON: high-freq raymarch detail flickers inherently — frame-blend feedback is the cure, not per-effect trimming. Also: don't coast on "hold" when a user complaint is unresolved.
- Iter 39 (on moire-eye-1, user: "it is twitchy"): de-twitch. moire-eye-1 uses RAW audio (no smoothing) so z-score/flux terms jump per-frame. Slowed gaze saccade to `vec2(sin(iTime*0.23),sin(iTime*0.19+1.3))*0.006` and REMOVED its `spectralFluxZScore` dart; removed `energyZScore` from pupil (bass 0.35→0.12); sphere bloom bass 0.6→0.25; mids-arms 0.5→0.25; catchlight bass 0.6→0.25. LESSON: on a no-controller shader, twitch = raw zScore/flux fed straight in — use Normalized at low gain or smooth, never zScore for continuous motion.
- Iter 36 (on moire-eye-1, user: "remove the horizontal scanline, reduce flickering"): REMOVED the VK amber scanline. Cut flicker sources: twinkle (`pow(sin(er*180+...+iTime*3),6)`) → static smooth outer glow band (knob_16 intensity); entropy hue-shimmer amplitude 0.25→0.08; removed `spectralFluxZScore` jitter from catchlight (bass-only now). Kept the moiré look the user approved. Saved + pushed via window.cranes.shader.
- **Iter 22 — MONOTONIC REWRITE (user mandate: "stop the flashing, most stuff monotonic unless bass-reactive; remove the scanline").** Added `controllers/moire-eye.js` holding forward-only phases (spin/flow/hue) + smoothed bass_env + slow drop_env + calm_env. Shader now: rotation via `A(spin_phase)`, palette via `H(nt + hue_phase)`, ripple via `sin(nt + flow_phase)` — all monotonic. REMOVED: VK scanline, high-freq twinkle (`sin(er*180...)`), beat ring, beat/flux catchlight snaps, entropy `sin(n*40)` shimmer, flux saccade dart, pitch-class collarette hue (jumps). Bass_env is the only direct-amplitude reaction (iris/pupil/airglow/catchlight swell); drop_env = one slow bloom per drop. knob_13 now = drop decay; knob_16 = outer stroma glow (was sparkle). Result: clean realistic blue eye (knob_2≈0.94), smooth evolution, zero strobe. Requires &controller=moire-eye.

- Iter 20 (TRIM, not add): tightened airglow. Screenshot at trebZ 1.0 showed the iter-13/14 airglow flaring into a frame-filling teal cloud + concentric ring artifacts. Narrowed gaussian (width 5→9, centre 0.52→0.50) and cut treble boost (0.10/0.06 → 0.05/0.05). Now a crisp shimmering rim that frames the eye instead of washing the frame. Lesson: wide gaussians × z-score boosts bloom into clouds on spikes — keep rim widths tight.
- Iter 19 (theme): Voight-Kampff scan line for "Replicant" (Sync24, Blade Runner ref). `scanY=fract(iTime*0.15); scan=exp(-pow((ec.y+0.5-scanY)*40,2))*irisMask*0.22; outc += vec3(0.95,0.55,0.20)*scan` — amber bar sweeps down the iris (~6.6s). NOTE: mid-tick the jam tab got remote-switched (remote=display) to moire-3d-1 with knobs; restored to moire-eye-1. The save had already landed on disk.
- Iter 18 (refinement): centroid → pupil-maw redness. `maw *= 0.10 + (1-spectralCentroidNormalized)*0.12` — the pupil tunnel glows deeper blood-red on DARK passages, dimmer on bright. Deliberately NOT a 4th bass effect (bass already = bloom+dilation+catchlight; avoided the stacking pitfall). Added on a dark deep-bass groove (bass 0.61, centroid 0.19).
- Iter 16 (refinement): flux → saccade flick. Saccade amplitude now `0.012 + max(spectralFluxZScore,0)*0.020` — the eye darts more on timbral motion (eyes glance at new sounds). Uses flux, the last motion-domain signal unwired. Feature map now spans every band + entropy/centroid/pitch/flux/energy.
- Iter 14: airglow → treble-reactive. Made the iter-13 halo pulse: `*(0.10 + max(trebleZScore,0)*0.10 + trebleNormalized*0.06)`. The cyan halo now shimmers on treble transients (trebZ 1.73 spike on Blue Airglow peak). Turns the static thematic element into an audio-coupled one. NOTE: the 0.00 audio reads iters 8/11/13 were just track gaps — capture is fine.
- Iter 13: "Blue Airglow" theme halo — `airglow = exp(-pow((er-0.52)*5,2))*0.12; outc += vec3(0.15,0.45,0.60)*airglow`. Faint cyan glow ring framing the iris in the void. Track-name palette move (Applefish/Lauge "Blue Airglow"); audio-independent (done on a silent gap). Cools the warm gold eye with a complementary blue rim.
- Iter 12: energyZ → pupil dilation. Added `+ max(energyZScore,0)*0.30` to pupilRad so the eye widens on energy rises/drops, not just bass. Fills the gap where bright bass-less peaks (energy 0.65, centroid 0.79, bass 0.16 on AstroPilot peak) left the pupil static. "Eye reacts to the drop."
- Iter 11 (polish, silence): catchlight bloom — split into `catchCore` (tight dot) + `catchGlow = exp(-pow(len*22,2))*0.4` (soft halo). Reads as a wet-eye glint with bloom instead of a hard dot. Audio-independent realism refinement, done on a silent gap.
- Iter 10: mids → stroma "arms" — `armZone = smoothstep(0.10,0.22,er)*smoothstep(0.40,0.26,er); col *= 1 + midsNormalized*armZone*0.5`. The mid-iris fiber band brightens with mids (iris-7 "mids=arms" model). Completes the band map: bass=core/pupil, mids=arms, treble=tips/sparkle. Added on a mid-dominant build (mids 0.88, energyZ 0.32 rising).
- Iter 9: calm breath — `calm = 1 - clamp(energyNormalized*2,0,1); col *= 1 + calm*0.12*sin(iTime*0.8)`. Slow ±12% iris swell that only appears on quiet passages (energy-gated), vanishes when busy. Added on AstroPilot calm pad (energy 0.10, mids 0.63). Independent: energy-gated + slow time-driven.
- Iter 8 (structural, silence): pupil maw — replaced flat-black `col *= pupil` with `mix(maw, col, pupil)` where maw = dim dark-red moiré. Pupil now has tunnel depth (iris-7's ribbed-maw signature). Chosen because all audio features read 0.00 (track gap) — no live signal to couple, so did the audio-independent Todo.
- Iter 7 (tuning, not a new effect): limbal frame — peak moved 0.44→0.48, darken 0.7→0.85, irisMask widened 0.52→0.44 ⇒ 0.56→0.46. Eye now reads as framed by a dark iris-rim instead of a faint thin ring. Screenshot before this confirmed gold petals + shimmer + catchlight all reading well.
- Iter 6: collarette ring — wavy bright crinkle at er≈0.24 (`collR = 0.24 + 0.012*sin(aa*15)`), the pupillary|ciliary boundary of a real iris. Hue tinted by pitchClassNormalized (cos triad). Anatomical realism + pitch domain (unused till now). Added on AstroPilot pitch 1.00 / beat passage.
- Iter 5: treble sparkle — `twinkle = pow(0.5+0.5*sin(er*180 + angle*9 + iTime*3), 6) * outerBand * trebleNormalized * 0.45`, cool-white, added to outc post-tanh. Added on AstroPilot "Distant Worlds" (trebZ 1.00 spike) — twinkling stars on the outer stroma, fits the cosmic theme. Independent domain (treble; bass/centroid/entropy already used).
- Iter 4: centroid → iris temperature. `warmth = 1 - spectralCentroidNormalized` shifts goldTint: dark/bassy = hot gold-red core (r 1.65, b 0.30), bright = cooler neutral gold (r 1.25, b 0.65). Independent from bass; gives the eye a brightness-tracking "mood". Added on a low-energy beat passage (centroid 0.29 = warm).
- Iter 3: micro-saccade — `saccade = vec2(sin(iTime*0.83)+0.4*sin(iTime*1.70), sin(iTime*0.71+1.3))*0.012` added to gaze. Autonomous eye drift; never loops (incommensurate freqs). Added on a calm melodic passage (Cosmic Alignment: energy 0.25, pitch 0.20) — eye life without frenzy.
- Iter 2: entropy stroma shimmer — `H(nt + spectralEntropyNormalized*sin(n*40.+t*6.2832)*0.25)`. Per-fiber hue jitter gated by entropy; calm passages keep the stable rainbow, chaotic passages (Cosmic Alignment: entropy 0.88, mids 0.90) make the iris fibers shimmer iridescent. Amplitude entropy-gated so it can't blow out.
- Iter 1: created by fusing `moire-3d-1` (hex-sphere tunnel + iter1 bass z-pulse) with iris-7 anatomy ported to screen-radial space — NO dodeca-bloom controller (uses raw `bassNormalized` etc.). Added: gaze (knob_12 X/knob_9 Y/knob_19 strength → camera rotation = eyeball turn), pupil aperture (knob_14, dilates on bass), limbal darkening, iris/void mask, warm-gold core tint (knob_2), bass-pulsed catchlight. Output: `clamp(tanh(col³) + catchlight)`.

## Forks
- `moire-eye-2 ← moire-eye-1` (iter ~20, /fork): frozen keeper snapshot with dialed-in knob preset (Carbon Based Lifeforms "Frog"). moire-eye-1 stays the live /vibej canvas. Note: display is remote-controller-driven (remote=display), so the hot-swap to -2 was overridden — kept live on -1 deliberately.
- `moire-eye-1 ← moire-3d-1` (iter 1): added iris-7 eye anatomy.
- `moire-eye-1 ← claude/wip/iris/7` (iter 1): borrowed pupil / limbal / gaze / catchlight / heterochromia-core concepts (re-implemented in screen-radial, controller-free).

## Design hypotheses for v(next)
- Radial raymarched moiré tunnels are an excellent iris-fiber substrate — reuse for other "cosmic eye" shaders instead of synthesizing fibers from scratch.
- Bass→pupil-dilation + bass→sphere-bloom together make the eye "breathe" convincingly on ambient swells. Keep both coupled but watch for over-dilation washing the iris.
- Next couplings to try (subtle): centroid→iris hue (bright music = greener/cooler), treble→outer-filigree sparkle, entropy→stroma shimmer, flux→catchlight glint.
