# the-coat-14 — Session Journal

## Status
Iter 38 (2026-04-27). New `/vj` run started on -14 after journal review. User explicitly chose -14 over the branch's nominal -13 to keep the elaborate fork (palette-shift, inky BG, IS_DROP tightened, zoom regime flipped). Audio via laptop mic + Spotify ("Cardio – Glass Petals, Drew Ray Tanner" at run start). knobs reset to 0 then knob_1 → 0.52 mid-setup. Cron: `b2f5626e`, target 180 iters @ 1/min. moveStyle: subtle.

## Forks
- `the-coat-14 ← the-coat-13` (iter 27). Music during fork: *No Signal – Sully*.
- **Iter 34 swap-back**: user asked to "ease back to the original aesthetic" → reverted live jam tab to `the-coat-13` (the figure-first baseline). the-coat-14's edits (palette-shift wiring, inky BG, IS_DROP tightening, zoom regime flip, knob_5 wiring, eyes sustained-energy) are preserved in this .frag for future reference but not in active use. Knob values preserved across swap; knob_3 now latent on -13 since CHAOS HALO was removed before the swap back.
- `the-coat-15 ← the-coat-14` (iter 64, 2026-04-27): live `/fork` during *Temptation – SIDEPIECE*. Captured painterly-groove preset (heavy feedback, max camera swagger, full palette rotation, nebula UP, fur thick, drop-zoom OFF). /vj run continues on -15 from iter 65 onward. See `the-coat-15-cool-moments.md`.

## Key lessons carried forward
(See `the-coat-3-cool-moments.md` → "Key lessons carried forward from prior VJ sessions" for the full corpus.)

**Fresh reminders for this fork:**
- CHAOS HALO fully removed. Don't re-add a concentric-rings-from-silhouette motif — user rejected it twice (once with characteristics, once outright).
- All prior vetoes still stand: CRYSTALLINE FACETS, MERCURY FLOW, RGB SPLIT, SCAN LINE, LIGHTNING, COSMIC SHOCKWAVE, INFINITY MIRROR, CONFETTI, VJ GRIT, TEARFALL.
- SUB RING, GROUND QUAKE, BLACK HOLE are drop_hit-gated — do not ungate.
- knob_3 is a free slot — wait for user direction before claiming it.

## Cool moments

### iter 58 (2026-04-27) — *Stay With Me – John Summit, Of The Trees* — Z-TRAIN second activation, palette-rotated
- **Audio fingerprint:** `bass 0.79 + bassZ 0.32 + treb 0.02 + entropy 0.19 + centroid 0.09 + roughness 0.17 + pitch 0.90 + pitchZ 0.62`. Z-TRAIN gate (`bass>0.6 + entropy<0.3 + centroid<0.3`) firing at ~0.61. `pitch_change: 0.425` mid-fire.
- **What worked:** First firing of Z-TRAIN under non-default palette (knob_3 = 0.835 — full cool/teal rotation from default amber). Beam color came out teal/cyan instead of amber — palette knob carrying through to a track-name-specific motif felt unified. **Knob storm + audio convergence**: 4 knobs moved >0.05 (knob_3 +0.31 palette big rotation, knob_8 -0.10, knob_9 +0.23 trails, knob_11 -0.10) AND Z-TRAIN gate firing AND pitch_change firing — third "convergence moment" of the run.
- **What was missed:** Z-TRAIN's beam at full strength looked smooth but didn't peak on `pitch_change` events — chord arrivals weren't visually correlated to the headlight passage. Added `* (1.0 + pitch_change * 0.6)` this tick so chord arrivals brighten the beam by up to 60%.
- **Design hypothesis for v(next):** The shader's "feature events" — `Z-TRAIN gate firing`, `pitch_change spike`, `IS_DROP transition`, `HEARTH gate triple` — should be detectable as discrete events with begin/end timestamps. Then any visual could subscribe to "amplify when 2+ events overlap". Right now we're hand-coding overlap multipliers per pair, which scales as O(n²) effects.

### iter 55 (2026-04-27) — *Another You – Armin van Buuren* — WARM HEARTH first activation in the wild
- **Audio fingerprint:** `mids 0.84 + centroid 0.11 + energy 0.17 + bass 0.54 + roughness 0.39 + pitch 0.90 + pitchZ 0.37 (climbing)` — exactly the warm-mid corner the journal flagged across 3 forks. WARM HEARTH gate (`smoothstep(0.45,0.75,mids) * smoothstep(0.50,0.25,centroid) * smoothstep(0.50,0.20,energy)`) hits ALL THREE sub-gates at 1.0 — first time in this run. `pitch_change: 0.44` chord arrival fired simultaneously.
- **What worked:** the long-flagged hypothesis paid off — amber glow extends from the silhouette during a calm vocal-led mid-bridge of *Another You* with knob_8=1.0 (DOOM RED) painting the BG crimson. Discovered that hearth at default `* 0.55` was getting washed out against the crimson BG → boosted to `mix(0.55, 0.95, knob_8)` so heavy DOOM RED scenarios get brighter amber. Also: the iter-41 pitch_change → HUE_BASE wiring was firing, so palette-aware effects (rim, sigil swirl, step ripple, ember col) got a coordinated chord-arrival shift simultaneously. **Multi-system convergence:** WARM HEARTH (warm-corner gate) + DOOM RED BG (knob_8) + pitch_change palette kick (controller) + hearth-DOOM-RED boost (this tick) all synchronized on one moment.
- **What was missed:** WARM HEARTH's color hue is `fract(0.08 + pitchClass * 0.04)` — basically locked to amber regardless of palette knob_3. Future: should probably HUE_BASE-link it like the chest_glow / sigil swirl, so palette knob rotates the warmth color too. Logging as todo.
- **Design hypothesis for v(next):** **detect convergence moments** — when 3+ feature-region gates fire simultaneously, that's a "thematic alignment" and the shader should know it. Bake a `convergence_score` uniform that other effects could amplify on alignment.


## Todo
- `[x] Pitch-change event detector — implemented iter 38 (2026-04-27). Live-watch: does the hue kick read as "harmony arrived"? Tune jump-threshold (currently 0.08 = ~1 semitone) + multiplier (currently 0.06) based on what reads.`
- `[ ] Hook pitch_change to a second visual channel beyond HUE_BASE — natural candidates: HUE_DROP for palette cohesion (parallel to iter 29's knob_3 logic), or a one-frame additive rim flash on chord arrivals.`
- `[ ] Effect-region DSL hypothesis — standing.`
- `[ ] Live-test iter-21 VJ WARM HEARTH on a warm-dark corner track — first run audio at iter 38 was bass 0.74 + centroid 0.20 + mids 0.26: WARM HEARTH gate is mids-dominant so it didn't fire. Watch for `mids > 0.5 + centroid < 0.3 + entropy < 0.2` track in the queue.`
- `[ ] Auto-wire knob_8 / knob_10 / knob_11 — currently unwired. First user-twist on any of these should claim them.`

## History of changes (post-fork)
- **Iter 28 (2026-04-22):** User nudged knob_3 to 0.189 (auto-wire signal — it had been free since CHAOS HALO removal). Rewired it as **global palette hue shift**: `HUE_BASE += knob_3` so 0 = no color change, 1 = full hue wheel. This revives the most-loved pattern from the-coat-11 (palette nav via knob_3 + knob_4). Chose this over inventing a new motif because the journal consistently shows users loving the "knob_3 = palette" affordance. User immediately cranked knob_3 to 0.992 — confirming the wiring was the right guess.
- **Iter 29 (2026-04-22):** Palette-cohesion polish: `HUE_DROP` now also includes `+ knob_3` so drop hits stay in the same palette region the user rotated to. Previously `HUE_DROP = fract(0.99 + centroid * 0.05)` was fixed near orange-red, and at knob_3 ≈ 1 with the base palette rotated to cyan/green, drop flashes snapped back to orange — dissonant. Now both base and drop hues rotate together with knob_3.
- **Iter 30 (2026-04-22):** User directive: "Assume if I'm changing a bunch of knobs at once that I want a greater change to the vibe of the shader." Encoded as a **many-knob heuristic** in SKILL.md §C.1 — 3+ knobs moved >0.05 since last snapshot = auto-promote that tick to dramatic. Applied it retroactively here (knob_2 +0.93, knob_5 -0.61, knob_3 +0.05). User pattern read: cranked climax back while killing drop-punch = "I want sustained climactic energy, not discrete drop hits." Dramatic move: flipped the zoom regime — `INTENSITY_ZOOM` coefficient raised from 0.3 → 0.55 AND added a `max(trebleZScore, 0.0) * 0.2` term so sustained-energy bright-trebly builds now push the camera in, not just bass-loud ones. Whole scene breathes harder with the music during long climactic passages. Audio was briefly 0.00 at tick start but came back live mid-tick; edit applied with fresh fingerprint.
- **Iter 31 (2026-04-22):** Many-knob heuristic triggered again — 5 knobs moved >0.05, all **downward** (knob_2 -0.72, knob_4 -0.44, knob_7 -0.46, knob_3 -0.20, knob_1 +0.17). User pattern read: global knob-down = "calm the scene, give the figure space." Dramatic move: tightened the fur-strand gate from `smoothstep(0.15, 0.55)` to `smoothstep(0.55, 0.90)` (strands only peak on truly chaotic passages, not every mid-busy track) and halved strand blend from 0.85 → 0.5 so the base coat color reads through even when they fire. Net: coat looks cleaner/less textured in most music, fibers still pop on genuinely chaotic moments. This is a **breathing-room** move that matches the knob-down pattern.
- **Iter 32 (2026-04-22):** Many-knob heuristic triggered (knob_2 +0.54, knob_5 +0.98, knob_3 -0.08). User pattern: climax cranked + drop-punch MAXED = "I'm expecting a drop and want everything to unload." Audio backs it up: energyZ 0.52 rising, energy 0.85, IS_DROP math was under-firing (smoothstep floor was 0.6, energyZ hadn't quite reached). Dramatic move: lowered the IS_DROP energyZ threshold floor from 0.6 → 0.35 and raised contribution from ×0.5 → ×0.75 so natural builds (energyZ 0.3-0.6 territory) now read as drop-confident earlier and harder. Cascades into every drop-gated effect: god rays, SUB RING, GROUND QUAKE, BLACK HOLE, rim width, drop-zoom, chest glow hue shift. Expected live effect: on *I'm the Truth*-style tracks with sustained high-energy + rising energyZ, the payoff now lands when the user feels the build, not a half-second late.
- **Iter 33 (2026-04-22):** Many-knob heuristic (7 knobs moved >0.05). User cranked several knobs DOWN (climax, zoom, drop-punch) + brought **knob_12 UP** from 0 (a knob that had NO wiring in this shader — twist was non-functional). Dramatic move: wired knob_12 as a **VJ INKY BG** block — dims L and mutes chroma outside the silhouette in oklab space, figure stays saturated and colorful, background goes inky/moody. Entropy modulator sharpens the effect so chaotic post-drop textures get crisper. Chroma-preserving operation in oklab avoids the gray-static feedback-loop pitfall from the-coat-11 outline block. User immediately cranked knob_12 from 0.236 → 1.0 — strong confirmation the wiring was the right guess and they wanted the inky affordance.
- **Iter 64 (2026-04-27, MANY-KNOB):** *Temptation – SIDEPIECE* (track shifted from *Simulation*). **Z-TRAIN corner + drop**: `bass 0.83 + bassZ 0.57 + entropy 0.30 + centroid 0.11 + energyZ 0.65 + drop_glow 1.0 saturated`. 3 knobs moved (knob_1 0.62→0.27 zoom back, knob_2 0.36→0.66 nebula up, knob_5 0.27→0 drop-zoom OFF). Pattern: user wants drops to land WITHOUT zoom-punch but with full visual weight. **Move:** added `+ pitch_change * 0.4` to RIM_BOOST. **pitch_change is now wired into 6 visual systems**: HUE_BASE rotation, eye intensity, Z-TRAIN beam, SIGIL SWIRL gain, WARM HEARTH multiplier, RIM_BOOST. Harmony arrivals now ripple across nearly every figure-defining channel. The de-facto event subscription pattern is essentially complete for `pitch_change`. Time to hold off on more pitch_change wirings — they're at the saturation point.
- **Iter 63 (2026-04-27, MANY-KNOB):** *Simulation – Glass Petals*. Audio calmed: `bass 0.68 + treb 0.08 + mids 0.62 + energy 0.47 + entropy 0.27 + centroid 0.16 + roughness 0.27`. **7+ knobs moved**: knob_1 0.06→0.62 (HUGE — tight zoom-in), knob_2 1→0.36 (no nebula), knob_5 0.73→0.27 (no drop-zoom), knob_15 → 0 (no drip), knob_4 → 0 (no eye wash), knob_11 0.98→0.68 (less STEP RIPPLE), knob_7 0.57→0.81 (fur up). **Pattern: "tight zoom on figure, BG quieted, palette full-rotated"** — user wants the figure foregrounded. **Move:** added `+ knob_1 * 0.5` to RIM_BOOST. At tight zoom the figure dominates the frame and rim definition matters more for silhouette readability against the smaller-relative BG. Cascades: any future zoom-in maxes get extra rim contrast automatically. **Lesson:** zoom level should drive figure-edge emphasis. Generalizes: anytime user takes a "show me the subject" gesture (tight zoom, calm BG knobs), figure-defining channels should compensate.
- **Iter 62 (2026-04-27, MANY-KNOB):** *Simulation – Glass Petals*. **Convergence moment**: `bass 0.88 + mids 0.83 + pitch_change 0.85 + drop_glow 0.61` — biggest synchronous low-mid+harmony combination of the run. **7 knobs moved**: knob_3 0→0.51 (palette mid), knob_5 0.44→0.78, knob_7 0.91→0.39 (fur thinned), knob_8 0.72→0.46, knob_11 0.80→0.63, knob_12 0→0.24, knob_1 0.31→0.09. **Move:** loosened WARM HEARTH energy gate ceiling from `smoothstep(0.50, 0.20, energy)` to `smoothstep(0.65, 0.20, energy)`. Track had `mids 0.83 + centroid 0.18` — perfect warm-corner signal — but `energy 0.54` was just above the inverted gate, so HEARTH didn't fire on a track where it should have been the main feature. New gate fires up to energy 0.65 with smooth fall-off. At current energy 0.54 the new gate ≈ 0.24 — visible amber bloom on this exact passage. **Lesson generalizing across iter 21/45/55/62 WARM HEARTH evolution:** the original "warm-mid corner" hypothesis was too narrow on energy. Real-world warm-vocal moments have mids 0.8+ AND moderate energy 0.5+ together — both are signs of a strong vocal/melody. The energy ceiling should accommodate that.
- **Iter 61 (2026-04-27, MANY-KNOB):** *Simulation – Glass Petals*. **Drop landing!** `energyZ 0.94 (peak) + bass 0.60 + entropy 0.32 + drop_glow 1.0 saturated`. **Orderly drop** (low entropy/roughness) — iter-47 RIM_BOOST chaos gate (`entropy > 0.6 + roughness > 0.5`) failed completely, so rim sat at baseline 2.5 + flux/bassZ during the run's loudest energy spike. **Move:** added `+ clamp(energyZScore, 0.0, 1.5) * 0.8` to RIM_BOOST. Now rim pumps up to +1.2 on peak energyZ regardless of chaos shape. Cascades: orderly drops AND chaotic drops both visibly pop the figure edge. **4 knobs DOWN this tick** (knob_6 1→0.39, knob_8 1→0.35, knob_12 → 0, knob_11 down) — user pulling visual layers back so the drop "breathes." My move pushes rim UP during the drop, complementing the user's reduction of overlays. **Pattern:** drop moments need rim emphasis regardless of chaos shape. The iter-47 chaos gate was too narrow.
- **Iter 60 (2026-04-27, MANY-KNOB):** *Sex, Drugs & Rock N Roll – Dr. Fresch* (still). Audio Z-TRAIN corner: `bass 0.78 + entropy 0.23 + centroid 0.08`. **5 knobs moved**: knob_3 PULLED BACK 1→0.50, knob_6 1→0.39, knob_5 1→0.61, knob_8 0.49→0.82, knob_11 0.43→0.11. Pattern: "calmer crimson" — settling after the iter-59 maxed-out config. **Move:** added `* (1.0 + pitch_change * 0.3)` to WARM HEARTH multiplier. **pitch_change now wired into 5 visual systems:** HUE_BASE rotation (iter 41), eye intensity (iter 57), Z-TRAIN beam (iter 58), SIGIL SWIRL gain (iter 59), WARM HEARTH multiplier (this tick). Composition at saturation but adding subscriptions to `pitch_change` doesn't add overlay surface — it just synchronizes existing effects on harmony arrivals. Effective "convergence amplifier" via pitch_change without a formal convergence_score yet.
- **Iter 59 (2026-04-27, MANY-KNOB):** *Sex, Drugs & Rock N Roll – Dr. Fresch*. **5 knobs moved**: knob_3 MAXED (palette → 1.0), knob_9 MAXED (feedback → 1.0), knob_7 + knob_12 up. User in "decadent neon trails" mode — full palette rotation + heavy smear. Audio mid-everything `bass 0.55 + mids 0.42 + entropy 0.45 + centroid 0.30 + roughness 0.43 + pitch 1.0` — no specific gate firing strong, just diffuse mid-energy. Composition at ceiling, refining over adding. **Move:** added `+ pitch_change * 0.5` to SIGIL SWIRL `mids_gain`. **pitch_change is now wired into 4 visual systems**: HUE_BASE rotation (iter 41), eye intensity (iter 57), Z-TRAIN beam brightness (iter 58), and SIGIL SWIRL gain (this tick). Harmony arrivals will now pulse simultaneously across palette, eyes, BG headlight, and coat swirl — synchronized "chord landed" reading. **Pattern note:** I'm building a de-facto subscription system for the controller's pitch_change event by hand-coding it into N effects. The iter-58 design hypothesis (event/subscription model) is increasingly validated. v(next) should formalize it.
- **Iter 58 (2026-04-27, MANY-KNOB):** *Stay With Me* (track shifted to *Sex, Drugs & Rock N Roll – Dr. Fresch* mid-save). Audio Z-TRAIN corner: `bass 0.79 + entropy 0.19 + centroid 0.09`. **4 knobs moved + Z-TRAIN firing + pitch_change firing = convergence moment** (logged as cool moment). Move: added `* (1.0 + pitch_change * 0.6)` to Z-TRAIN beam emission so chord arrivals visibly brighten the headlight. **First non-default-palette Z-TRAIN viewing** — knob_3 = 0.835 made the beam teal/cyan, demonstrating the iter-44 design choice (HUE_BASE-tinted) carried through cleanly.
- **Iter 57 (2026-04-27):** *Stay With Me*. Audio warm-corner-adjacent: `mids 0.63 + centroid 0.08 + energy 0.37 + entropy 0.39`. `pitch_change: 0.614` firing strong (chord arrival). The iter-41 wiring rotates HUE_BASE by `pitch_change * 0.06` ≈ 13° at peak — too subtle to read as "harmony arrived." **Move:** added 4th distinct response — `+ pitch_change * 0.3` to the eye intensity formula. At pitch_change = 0.61 eyes get +0.18 — a real flash that reads as a discrete "harmony moment." Doesn't fight palette stability (which is the iter-41 hue kick's job). **Eye system now has 4 audio-region channels:** bright-sustained, calm-vocal, chaotic-bright, harmony-arrival (this tick) — plus baseline + drop-hit + treble-snap. Continuing to feel ad-hoc; design hypothesis from iter 53 stands.
- **Iter 56 (2026-04-27):** *Stay With Me – John Summit, Of The Trees*. Audio: `bass 0.61 + treb 0.02 + mids 0.49 + energy 0.18 + entropy 0.35 + centroid 0.15`. Calm warm-corner-adjacent. **Resolved iter-55 todo:** WARM HEARTH color was hard-coded amber. Replaced `fract(0.08 + pitchClassNormalized * 0.04)` with `fract(HUE_BASE + 0.1)` — palette-aware, with a 0.1 hue offset giving a "warm complement" of the user's rotated palette. At knob_3 = 0.27 (current): HUE_BASE ≈ 0.05 + offset 0.1 = 0.15 (still amber/orange — matches old behavior). At knob_3 = 0.7: HUE_BASE ≈ 0.5 + offset = 0.6 (purple-pink, still feels "warm-of-cool-base"). HUE_BASE already includes the pitchClass + mids drift, so the music-driven character is preserved. **System lesson:** anytime an effect's color is hard-coded but everything else uses HUE_BASE, that's a future palette-coherence bug. Audit pass on this shader for other hard-coded hues would be a worthwhile separate tick.
- **Iter 55 (2026-04-27):** *Another You* → *Stay With Me – John Summit, Of The Trees* (track shifted mid-save). Audio at edit-time: warm-mid corner `mids 0.84 + centroid 0.11 + energy 0.17 + pitch 0.90 + pitchZ 0.37`. **Cool-moment captured (see Cool moments section).** WARM HEARTH gate finally fired all-three at 1.0 — long-standing journal hypothesis paid off. Edit: changed `col += hearth_col * hearth * warm_gate * breath * 0.55` to `* (0.55 + knob_8 * 0.4)` so the amber stays visible against heavy DOOM RED crimson BG (was getting washed out). At knob_8 = 1.0 hearth output is 1.7x brighter; at knob_8 = 0 unchanged. **Brief MCP disconnect this tick** — Chrome extension dropped and reconnected; one retry got us back. Cron continued normally. **New journal todo:** WARM HEARTH hue is hard-coded amber (`fract(0.08 + pitch * 0.04)`); should be HUE_BASE-linked so user palette rotation moves the warmth color.
- **Iter 54 (2026-04-27):** *Another You* (10+ ticks now). Audio mid-everything: `bass 0.60 + treb 0.32 + mids 0.46 + energy 0.52 + entropy 0.65 + centroid 0.56 + roughness 0.40`. Knobs settled (only 2 small moves). **Subtle BG-layering polish:** STARFIELD adds to bg, then NEBULA adds *additively* on top, so dense nebula made stars *brighter* not occluded — opposite of natural depth feel. Multiplied star intensity by `(1.0 - knob_2 * 0.5)` so dense nebula partly hides the stars. At current knob_2 = 0.54 stars are 73% bright; at knob_2=1 they're 50% bright. Restores "stars behind haze" depth illusion. **Cross-feature lesson companion to iter 52:** when two BG effects share the canvas region, AND one is meant to be "in front of" the other (nebula > stars by metaphor), the front effect should reduce the back one's visibility, not just add on top. v(next) should declare BG layer order with explicit "occlusion" relationships.
- **Iter 53 (2026-04-27):** *Another You* — chaotic-bright passage. Audio: `treb 0.57 + trebZ 0.74 (HUGE) + entropy 0.82 + centroid 0.83 + roughness 0.70 + flux 0.54 + energy 0.52 + energyZ 0.10`. Knobs settled (only 2 small moves). Subtle tick. **Audit revealed gap:** at chaotic-bright fingerprint the eye intensity formula had no firing channel — iter-26's bright-sustained gate needs `energy > 0.5` (we're marginal at 0.52, smoothstep(0.5,0.85) ≈ 0.07), iter-45's calm-vocal needs `energy < 0.5` AND `roughness < 0.4` (this passage at 0.70 fails). RIM_BOOST entropy term WAS firing strong (~+0.75) but eyes sat quiet — figure-edge crackling without eye-presence is dissonant. **Move:** added 3rd parallel eye gate `+ smoothstep(0.6, 0.85, spectralEntropyNormalized) * smoothstep(0.6, 0.85, spectralCentroidNormalized) * 0.6` so high-entropy + high-centroid passages also wake the eyes. Track hits both: ~0.88 * 0.92 * 0.6 = +0.49. Mutually-mostly-exclusive with calm-vocal (centroid gate) and partially-overlapping with bright-sustained (energy threshold). **Three-channel eye system now:** bright-sustained (iter 26), calm-vocal (iter 45), chaotic-bright (iter 53). Each covers a distinct corner of the audio feature space. **Design hypothesis for v(next):** the eye system should be a first-class subsystem with declarative gates per emotion-corner, not a stack of additive smoothsteps. Three-channel layering is starting to feel ad-hoc.
- **Iter 52 (2026-04-27, MANY-KNOB):** *Another You – Armin van Buuren* (still). Audio lull: `bass 0.67 + treb 0.20 + mids 0.59 + energy 0.35 + energyZ -0.22 + entropy 0.53 + centroid 0.46 + roughness 0.38`. **8 knobs moved >0.05** — "loaded build configuration": user MAXED knob_5 (drop-zoom 0.05→1) + knob_7 (fur 0.37→1) + knob_8 (DOOM RED → 1) + UP knob_2 (nebula fog 0→0.54) + knob_6 (camera tilt 0.41→0.85) + knob_14 (sigil swirl 0.1→0.4). Visual layers piling on while audio is calm = anticipating return of energy. **Move:** identified BG palette conflict — DOOM RED maxes the BG to crimson, but **NEBULA FOG colors are purple `(0.35, 0.15, 0.55)` + blue-teal `(0.10, 0.35, 0.70)`** — directly clashing. Added `warm_shift = smoothstep(0.5, 1.0, knob_8) * 0.7` and mix nebula colors toward `(0.55, 0.10, 0.10)` and `(0.65, 0.20, 0.10)` (warm reds) when DOOM RED is high. Below knob_8 = 0.5 nothing changes; at knob_8 = 1.0 nebula is 70%-warm-shifted. Unifies BG palette during DOOM RED moments. **Cross-feature harmony lesson:** when two effects share the same canvas region (here: BG layer), their default colors should not directly fight. The user has to manually balance them now via knobs; future shaders should declare color-palettes-per-region so this kind of cross-feature shift is automatic.
- **Iter 51 (2026-04-27):** *Another You* still (5+ ticks now). Audio: `bass 0.68 + treb 0.19 + mids 0.44 + energy 0.58 + energyZ 0.41 (rising) + entropy 0.56 + roughness 0.40`. Build/sub-drop territory. **Tuning my own iter-48 GROOVE BREATH:** added a 4th gate term `* smoothstep(0.6, 0.2, max(energyZScore, 0.0))` so the breath fades out as energyZ climbs. Rationale: GROOVE BREATH is for **calm grooves**, not builds — it competes for visual attention with the legitimate build-up effects (drop-zoom, rim-boost, IS_DROP cascades). At current energyZ 0.41 the new gate is ~0.48 (half-suppressed); at energyZ ≥ 0.6 (drop) it's 0. At energyZ ≤ 0.2 (calm) it's 1.0 (no change). Self-tuning lesson worth carrying: **whole-frame ambient effects should bow out during builds**, not stack with them.
- **Iter 50 (2026-04-27):** *Another You – Armin van Buuren* still. Audio: `bass 0.71 + bassZ 0.56 (spike) + treb 0.08 + mids 0.48 + entropy 0.37 + centroid 0.15 + roughness 0.18` — bass-spike on calm passage. **7 knobs moved >0.05** — many-knob, but composition is at the ceiling so subtle again. User pattern: knob_5 (drop-zoom) UP 0.18→0.79 + knob_7 (fur) MAX + knob_10 (ground quake) UP + knob_12 (inky bg) UP + knob_14 (sigil swirl) UP, all while keeping knob_9 ~0 (still crisp). Building visual layers atop crisp base. **Move:** Sigil Swirl `mids_gain` formula previously was `0.4 + midsNormalized * 0.8` — mids-only response, so bass spikes had no effect on swirl intensity. User just turned on the swirl while bassZ was spiking 0.56 → mismatch. Added `+ clamp(bassZScore, 0.0, 1.5) * 0.5` so swirl punches on bass kicks. Track bassZ 0.56 → +0.28 to mids_gain (vs baseline ~0.78) — noticeable but not blowing out. Cascades: any future track with bass kicks now visibly thumps the swirl.
- **Iter 49 (2026-04-27):** *Another You – Armin van Buuren* still. Audio: `bass 0.71 + treb 0.16 + mids 0.52 + energy 0.40 + entropy 0.29 + centroid 0.25 + pitch 1.00 + pitchZ 0.49 + roughness 0.24` — calm orderly bass passage, pitch climbing. **8+ knobs moved >0.05** — would normally trigger dramatic upgrade, but **deliberately overrode to subtle**: composition is at the ~6-overlay ceiling per skill warning, last 4 ticks all added/extended motifs (RIM_BOOST entropy term, GROOVE BREATH, EMBER RISE, Z-TRAIN, STEP RIPPLE), and the user's pattern this tick is **180-degree opposite** of last ("crisp/clean" vs "painterly groove"): knob_9 ZEROED (no feedback), knob_15 → 0.06 (no drip), knob_2 → 0.17 (no nebula fog), knob_6 → 0.15 (no swagger). **Move:** widened the `feedback_amt` formula endpoints. Original `mix(0.45, 0.05, knob_9)` for BG and `mix(0.15, 0.03, knob_9)` for silhouette left knob_9=0 still rendering at 55% prev / 85% prev — not actually crisp. Changed to `mix(0.85, 0.05, knob_9)` and `mix(0.70, 0.03, knob_9)` so knob_9=0 truly delivers crisp (15% prev BG, 30% prev silhouette) while knob_9=1 still hits the same heavy-smear endpoint. **Why this isn't disruptive:** when user wants smear, they crank knob_9 — that endpoint didn't move. The change only improves "knob_9=0" mode, exactly the regime user is in *right now*. Cascades into all future "crisp" requests. **Note:** journal Status/Wired-knob list said `knob_2: climax dampener` but that wiring is stale — the active wiring is `nebula fog density` (line 428, iter 33-area). Worth correcting next time the journal Status is rewritten.
- **Iter 48 (2026-04-27, MANY-KNOB DRAMATIC):** *Another You – Armin van Buuren, Mr. Probz* — post-drop calmer. Audio: `bass 0.63 + treb 0.15 + mids 0.57 + energy 0.43 + entropy 0.49 + centroid 0.29 + pitch 0.91 + pitchZ 0.45 (climbing) + roughness 0.38`. **10 knobs moved >0.05 — biggest storm of the run.** User MAXED knob_9 (feedback 0.34→1.0, heavy smear opt-in) + knob_6 (camera tilt 0.83→1.0 swagger MAX), pulled knob_2 (climax) WAY down 1→0.378, knob_5 (drop-zoom) → 0, knob_4 (eye wash) down 0.48→0.30, knob_8 (DOOM RED) UP 0.15→0.748, knob_14 (sigil swirl) on. **Pattern: "painterly groove mode"** — heavy trails + swaying camera + painted coat + reduced drop punch. Different from the prior tick's "intense figure-focus drop" by intent. **Move:** new **VJ GROOVE BREATH** block — slow 0.5Hz full-frame brightness breath (`0.5 + 0.5 * sin(time * 1.6)`) gated by triple smoothstep: energy bell-curve peaking at 0.4 + mids dominance >0.4, multiplied by knob_6 so camera-tilt-swagger drives the breath depth. Additive only (`vec3(0.05, 0.04, 0.025) * breath * gate * knob_6`) — never darkens, no feedback risk. Track hits the bell-curve perfectly: energy 0.43 = peak gate ≈ 0.95, mids 0.57 = gate 0.68. Should pulse a gentle scene-wide glow at sin(1.6t) Hz with ~6% brightness amplitude — visible only when the scene is otherwise calm and grooving. **Why the dramatic upgrade got a small additive instead of a big motif:** all three drop-style motifs (rim-amp iter 47, EMBER iter 46, Z-TRAIN iter 44) already exist; this fork now has 6+ overlay effects already so adding more would saturate (skill warns ~6 max). The breath is a *whole-frame ambient layer* — doesn't compete with overlays.
- **Iter 47 (2026-04-27, MANY-KNOB DRAMATIC):** *Another You – Armin van Buuren, Mr. Probz* — DROP MOMENT. Audio: `entropy 0.88 + roughness 0.70 + energy 0.84 + energyZ 0.82 (RISING) + flux 0.85 + treb 0.53 + trebZ 0.63 + centroid 0.69` — peak chaos drop. **7 knobs moved >0.05** — biggest storm of the run. User MAXED knob_3 (palette → 1) + knob_4 (eye wash 0.42→0.94) + knob_5 (drop-zoom MAX) + knob_7 (fur 0.43→0.86), but **ZEROED knob_11 (STEP RIPPLE OFF)** and pulled knob_2 (climax dampener) DOWN from 1→0.567. Pattern: "intense figure-focused presentation, no STEP-RIPPLE clutter on this drop, tempered climax." **Move:** extended `RIM_BOOST` macro with `+ smoothstep(0.6, 0.85, spectralEntropyNormalized) * smoothstep(0.5, 0.85, spectralRoughnessNormalized) * 1.5` — rim crackles harder ONLY at chaos peaks (entropy > 0.6 AND roughness > 0.5). Current track hits both gates: smoothstep contribution ≈ 1.0 * 0.57 = 0.57 → +0.85 to RIM_BOOST baseline 2.5 = ~3.4 (pre-existing flux/bassZ terms add more). On orderly tracks both gates near zero → no contribution. **Why this fits the user's intent:** they want the figure to be the focal point during this drop (not overlays), so amplifying the rim during chaos is exactly the right direction — focuses attention on the silhouette edge, doesn't add new motifs, doesn't risk re-triggering aura/lightning vetoes. Single-line edit but cascades through every drop-chaos moment going forward.
- **Iter 46 (2026-04-27, MANY-KNOB DRAMATIC):** *Everything Is Burning – Claude VonStroke, Nala*. **Track-name screams fire.** Audio: `bass 0.38 + treb 0 + mids 0.62 + centroid 0.09 + energy 0.41 + roughness 0.35` — mid-vocal warm-low corner. **5 knobs moved >0.05** (knob_3/4/8/9/11). User pattern: knob_8 maxed (DOOM RED → 1) + knob_4 came up 0→0.43 (eye wash on) + knob_9 down (less smear). User saying "make the figure burn." **Dramatic motif:** new **VJ EMBER RISE** — 6 deterministic ember sparks rising from y=-0.7 to y=0.5, each at slightly different speed (0.16-0.34 per sec) with a phase-driven `pow(1-phase, 1.5)` brightness fade so they trail off as they rise. Lateral wobble via `sin(time*1.7 + fi*1.3) * 0.04` for organic feel. Color: orange-amber `vec3(1.0, 0.55 + 0.2*sin(time*2), 0.1)` — flickers between bright orange and amber. **Smooth math, no hash, no feedback** — respects gray-static lessons. Lower-half-masked (`smoothstep(0.2, -0.3, uv.y)`) and silhouette-aware (`(1.0 - silhouette)`). Gate locks to mids > 0.45 + centroid < 0.30 — the warm-low fingerprint. Track has mids 0.62 + centroid 0.09 = both gates near full → embers should fire strong immediately. Inserted between GROUND QUAKE and STEP RIPPLE in col-layer.
- **Iter 45 (2026-04-27):** *Another You – Armin van Buuren, Mr. Probz* — vocal-trance ballad. Audio: `bass 0.76 + treb 0.07 + mids 0.60 + energy 0.17 + centroid 0.16 + roughness 0.22` — calm vocal-led corner. **4 knobs moved >0.05** (knob_3/4/7/9) but I picked subtle anyway: the audio is so quiet that a heavy new motif would feel wrong. **Move:** added a parallel "calm-vocal" channel to the eye intensity formula at line 535. The iter-26 sustained-energy gate (`energy > 0.5 + centroid > 0.4`) fails completely on this calm fingerprint, so the eyes sat dim during the most emotional passage. New term: `smoothstep(0.4, 0.75, midsNormalized) * smoothstep(0.5, 0.15, energyNormalized) * smoothstep(0.4, 0.15, spectralRoughnessNormalized) * 0.7`. Mutually exclusive with iter-26 at energy=0.5 boundary so they don't double-fire. Track fingerprint hits all three sub-gates this tick (mids 0.60 → 0.8, energy 0.17 → 1.0, roughness 0.22 → 0.72) so eye contribution is ~0.40 — should make the eyes wake up immediately on this kind of vocal ballad. Hypothesis confirmed if it lands: **emotional vocal passages were under-served by the eye system.**
- **Iter 44 (2026-04-27, MANY-KNOB DRAMATIC):** *Z-Train – ZHU* (track shifted to *Another You – Armin van Buuren* mid-save). Audio: ULTRA-CALM bass-only `bass 0.75 + centroid 0.06 + entropy 0.11 + energy 0.06 + roughness 0.09 + pitch 0.82 + pitchZ 0.40`. **6 knobs moved >0.05** (knob_5/7/8/9/11/15) → many-knob dramatic upgrade. User maxed knob_7 (fur thickness MAX 0→1) + knob_15 (drip MAX) + knob_8 (DOOM RED 0.55→0.9), pulled knob_9 (feedback) DOWN from 0.95→0.56. Pattern read: "thick fluffy crisp blood-soaked figure dripping wet, less smear." **Track-name dramatic move:** new VJ Z-TRAIN block — slow horizontal headlight beam (10s sweep cycle, `mod(time*0.10, 1.4) - 0.7`) crossing the BG. Triple smoothstep gate locks it to ZHU's exact corner: `bass > 0.6 + entropy < 0.3 + centroid < 0.3`. Palette-tinted via HUE_BASE so knob_3 rotation drives the headlight color. Smooth math, no feedback, BG-layer (passes behind figure). Audio fingerprint hit all three gates simultaneously this tick — should have fired immediately. Track shifted to *Another You* mid-save; the Z-TRAIN gate is now zero on the new track (Armin = treble-bright trance, not bass-deep ZHU corner).
- **Iter 43 (2026-04-27, MANY-KNOB DRAMATIC):** *1, 2 Step (Supersonic) – SIDEPIECE, Lee Foss*. **Many-knob storm** (~9 knobs moved >0.05). User pulled DOWN every chaos knob (knob_5/6/7/8/10/14) but MAXED knob_2 (climax 0→1), knob_3 (palette 0.58→1), knob_15 (drip 0.5→1). Pattern: "calm the chaos, max the three things I care about" — declarative aesthetic narrowing on a dance/house track. Audio supports it: `bass 0.37 + mids 0.54 + treble 0.35 + entropy 0.75 + roughness 0.72 + energy 0.87 + energyZ 0.57 + centroid 0.47` — busy but not bass-heavy. Track-name **"1, 2 Step"** = beat-rhythm theme. **Dramatic move:** wired previously-unwired **knob_11 → VJ STEP RIPPLE**. New block inserted before the CHAOS-HALO-removed comment: horizontal beat-driven wave radiating laterally from chest (`pow(0.5 + 0.5*sin(abs(uv.x)*18 - BEAT_PHASE*6), 4)`), gaussian band fall-off (`exp(-dy^2*80)`), HUE_BASE-tinted (palette-aware so knob_3 rotation drives the ripple color), energy-gated (`smoothstep(0.3, 0.8, energyNormalized)`), silhouette-masked (`(1.0 - silhouette)`), knob_11 intensity-gate. Smooth math only — no hash, no prev-frame, safe under feedback. **Completes auto-wire queue: unwiredKnobs is now []**. Every knob 1-15 (except knob_13 which was always wired via controller) is now meaningful.
- **Iter 42 (2026-04-27):** *Armageddon – Dr. Fresch* (track shifted to *1, 2 Step – SIDEPIECE* mid-save). Audio mids-up bridge: `bass 0.60 + mids 0.66 + centroid 0.14 + treb 0`. No knob storm — subtle tick. Continued auto-wire queue: **knob_10 → GROUND QUAKE gain** via `mix(0.55, 1.4, knob_10)` on the final emission. Preserves the iter-25 drop-gating (figure-first stays intact) but lets the user dial floor-rumble amplitude up to ~2.5x for tracks that want a heavier earth-shake. User had knob_10 = 0.61 from prior storm — wiring takes effect at ~70% of max gain immediately. Notable controller-side: `pitch_change` fired 0.85 this read — strongest harmony arrival logged so far. unwiredKnobs now [11].
- **Iter 41 (2026-04-27):** *Armageddon – Dr. Fresch*. Heavy bass + ultra-low centroid (0.11) + treble 0 + roughness 0.22 — "doom-bass" corner. Track-name lock-in: wired previously-unwired **knob_8 → VJ DOOM RED** block. Inserted right after the IS_DROP god-rays addition: `bg = mix(bg, bg * vec3(1.4, 0.15, 0.10) + vec3(0.18, 0.0, 0.0) * bassNormalized, knob_8 * 0.55)`. R-shifted BG with a bass-pulsed crimson lift; faded in via knob_8 with a 0.55 ceiling so it never fully replaces the original BG. User had moved knob_8 from 0 → 0.52 in the prior storm — auto-wire fires it on a thematically perfect track. unwiredKnobs now [10, 11]. (Many-knob storm ended this tick — only knob_1 moved >0.05; subtle mode stays.)
- **Iter 40 (2026-04-27, INFRA MOVE):** *Movin' That – Capozzi*. User explicitly requested: "Can we do updates without freezing the renderer briefly like it does now". Diagnosed: `Visualizer.js:181` called `createProgramInfo` (sync) inside the per-frame `render()` fn — every shader hot-swap blocked the loop ~100-500ms while GLSL compiled + linked + status-checked the 875-line shader. **Fix:** swapped to twgl's `createProgramInfoAsync` which uses the `KHR_parallel_shader_compile` extension to poll completion without blocking. Added `pendingShaderSource` state so duplicate HMR pulses for the same source don't queue, and so rapid edits (A→B→C in <1s) discard the stale in-flight compile and only land on the latest. **Bonus UX win:** removed the `programInfo = null` branch on compile error — the renderer now keeps drawing the last-good shader when the user types a typo, instead of going blank. Tested live: renderer kept rendering uninterrupted, no error state, frameCount continued advancing during the swap. Not a shader edit so no /__save-shader call this tick. **Many-knob storm in progress** during this turn (12+ knobs moved >0.05 including knob_8/10/11 which are unwired) — queued for iter 41 to do the dramatic shader-side move + auto-wire knob_8/10/11.
- **Iter 39 (2026-04-27):** *All Eyez On Me – Capozzi*. Audio fingerprint: `bass 0.24 + bassZ -0.46 + treb 0.27 + trebZ 0.56 + mids 0.60 + entropy 0.63 + centroid 0.42 + pitch 1.00 + energy 0.38 + flux 0.06`. Treble spike was the dominant signal but EYE_WASH_STRENGTH only summed energyZ + bassZ — both negative this passage, so eyes sat dim during the brightest moment of the track. **Track-name → motif** archetype: added `+ clamp(trebleZScore, 0.0, 1.0) * 0.4` term to EYE_WASH_STRENGTH so the eyes wake on bright treble spikes too. Subtle move; the existing 0.5 energyZ + 0.2 bassZ terms stay. User mid-tick moved knob_1 0.52→0.346 + knob_2 0→0.315 (sub-3 knobs, no dramatic upgrade).
- **Iter 38 (2026-04-27):** New `/vj` run starts. **Implemented the long-standing pitch-change-event-detector hypothesis** flagged as standing across -6, -8, -14 todos. (a) Extended `controllers/the-coat.js` to maintain `prevPitchClass` + emit a new `pitch_change` 0-1 transient: detect circular pitch delta unwrapped to [-0.5, 0.5], fire `min(jump * 5, 1.0)` only when |Δ| > 0.08 (~1 semitone) AND prior pulse has decayed below 0.3 (hysteresis), exponential decay 0.85/frame (~0.5s half-life). (b) Added `uniform float pitch_change` to the shader and hooked it as `+ pitch_change * 0.06` on `HUE_BASE` so chord changes nudge the palette ~22° on the wheel. Subtle by design; if it reads weak, the next tick can multiply HUE_DROP and/or rim-flash. **Dev-server restart was required** mid-tick because Vite's `vite.config.js:19` excludes `controllers/**` from the watcher, so its transform cache served stale bytes even on `?t=...` cache-bust requests. After restart, `pitch_change` flowing as 0.39 within seconds — detector caught a chord change immediately on *Cardio – Glass Petals* (audio: bass 0.46 + mids 0.54 + pitch 0.96 + pitchZ 0.35). User had moved knob_1 to 0.52 during setup; logged into knobSnapshot.

## Design hypotheses for v(next)
(carry forward from -12 / -13 journal)
