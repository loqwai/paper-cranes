# taco-kandi-6 — Session Journal

## Status
Iter 45 of /vibej run. **NEBULA FOG was the persistent circular bg flash** — found via flash-hunter (16-pt pixel sampler + audio-feature snapshots). Removed. Same iter: chrome rim hue de-magenta'd (bounded short-arc through indigo, never magenta); CORE_HUE/CORONA_HUE locked to plasma-event-horizon/4 anchors (orange + deep blue-violet only). User has been clear: "focus on the taco logo, based off of the borders or outline and making sure it is discernable" + "Not just palette - the 'forms'" — next moves port plasma's RADIATION_WAVES emanating from the SILHOUETTE OUTLINE (not center).

## Forks
- `taco-kandi/6 ← taco-kandi/5` (iter 38, 2026-05-09, music: *Windhorse — Of The Trees, Sophie Marks*): user rejection list applied. See `journals/taco-kandi-5-cool-moments.md` for iters 35-38 history.

## Removed effects (do not re-introduce — user explicitly rejected)
- **Beat flash** (silhouette × 1.25 on beat_pulse) — read as "green flash" with the plasma palette
- **Rim zap** (white-cyan flash on ink lines per beat) — same green-flash issue
- **Horizon scan bar** (thin horizontal sweep on bright/rising-energy passages) — too literal
- **VJ Drip** (sphere falling from taco_center) — distracting
- **GREEN BG FLASH** (iter 40) — gone after sigil swirl removal. The "green flashing in the background" was the SIGIL SWIRL leaking through — its hsl2rgb hue cycle landed on green at certain pitchClass + time combos, brightening the plasma_col which fed the bg.
- **SIGIL SWIRL** (iter 40, FORK FORK FORK rejection) — user: "I do not like This spinning, multicolored thing we have right now" + "I've seen variations of this effect a lot. Remember I don't like it." User confirmed "Much better" after removal. **NEVER re-introduce angular spirals with continuous hue rotation.** Pattern recorded: rotating hue around an angular sweep with hsl2rgb is on the global rejection list.
- **God rays** (iter 39, removed iter 40) — flashed on bass_smooth + drop_glow; CORONA_HUE landed in green region. Don't pulse intensity gates on every kick.

## User-stepped-away iteration plan (iter 39+)
Direction: **plasma-style radiating, music-reactive, evolving**. NOT center-radial waves (rejected). NOT additive overlay rings. The plasma series gets its "radiating" feel from:
1. **Volumetric raymarched accretion disk** at the event horizon (already have inside taco)
2. **Gravitational lensing envelope** that warps space toward center
3. **Photon ring** at a specific radius that pumps with bass
4. **Hot core glow** that radiates outward via additive bloom
5. **Event horizon void** that fades to black at the corners

Plan for iter 39+ (in order, one move per tick):
- iter 39: GOD RAYS emanating FROM the silhouette outline (logo-aware radiating beams), gated by drop_glow + treble. Anchored to oklch CORONA family.
- iter 40: Plasma-style volumetric exterior — extend the accretion disk OUTSIDE the silhouette so the energy radiates from the taco's edge.
- iter 41+: Slow morphing radial effects (sub-Hz frequencies, "organism breath" pattern from plasma jam).

## Cool moments
(pending — capturing live)

## History of changes
- **Iter 47 (2026-05-09) — *Odds & Ends / Late Night Radio* — extended OUTLINE_RADIATION reach; bands visible** — Iter 46's radiation horizon was too tight (d_max=0.061, falloff e^-24d crushed bands within first 5% screen). Extended to d_max=0.144, falloff e^-10d. Wave density bumped from `mix(80, 140, fract(seed))` to `mix(80, 200, fract(seed))` — at d_max with N=80 we get 1.8 cycles ≈ 3 bands; with N=200 we get 4.6 cycles ≈ 9 bands (mandala territory). Color gradient near-to-far: CORONA (cool blue-violet) → CORE+0.6 (warm orange-red) — taco haloed in cool that warms outward, mirroring plasma's photon-ring → core gradient but applied to OUTLINE distance instead of CENTER distance. Screenshot captured: clear blue-violet halo + warm outer breath. Logo unmistakable. **Form landed.**
- **Iter 46 (2026-05-09) — *Paranoid / Layton Giordani* — OUTLINE RADIATION FORM (the headline plasma-form port)** — User flag chain: "Not just palette - the 'forms'" + "focus on the taco logo. based off of the borders or outline and making sure it is discernable" + "Now I'm leaving for real. Go above and beyond to impress me!" **Move:** ported plasma-event-horizon/4's signature RADIATION_WAVES form, but the wave argument is distance-from-the-SILHOUETTE-OUTLINE, not distance-from-center. Plasma's gravitational ripples become OUTLINE ripples expanding outward from the taco's contour. Built `getOutlineDistance(uv)` helper: binary-search across 6 logarithmic radii × 8 angles, returns approximate UV-space distance from the silhouette boundary. Wave function: `OUTLINE_WAVE(d) = sin(d * N - iTime * RATE * TAU) * 0.5 + 0.5` where phase is iTime-only (audio in phase = strobe per plasma-event-horizon iter 19b lesson). Audio gates AMPLITUDE only via RADIATION_LOUDNESS = bass_smooth*0.7 + midsN*0.5 + 0.18 (baseline floor so always-radiating). RADIATION_GAIN = 0.30 * (1 + entropyN*0.35 + MONSTER_BASS*0.40). Concurrent: added MONSTER_BASS gate (sub-bass-only "lurking monster" pocket — bass HIGH + mids/treble/energy LOW + dark centroid). RADIATION_RATE = mix(0.15, 0.65, knob_5) — slow 0.15→0.65 Hz "powerful slowish" rate. Bounded mix() blend, max gain 0.42. **Banding-impossible by construction.** Also: WARM HEARTH bug fixed — was `hsl2rgb(fract(seed2*0.3 + 0.1), 0.85, 0.45)` which could land on green for some seeds. Replaced with `oklch2rgb(0.65, 0.18, CORE_HUE + seed2*0.05)` — locked to plasma family, green impossible. **Pattern recorded:** plasma's radial form, applied to OUTLINE distance instead of CENTER distance, becomes a logo-aware halo-radiation. Same physics, traced along the brand. The taco is the gravitational well; its contour is the photon ring; gravitational waves ripple outward from the silhouette.
- **Iter 45 (2026-05-09) — *Honeydust / Of The Trees, Kala* — FOUND THE FLASH FOR REAL: NEBULA FOG. Plus magenta-border + plasma-anchor palette fix.** — User: "I saw the flash several times for sure over the last 40s." Built a flash-hunter (`window.__flashHunter`): 16-point bg-pixel sampler at 64×64 downsample, paired with audio-feature snapshots, capturing peak `maxBg` per frame. Hunter's peakBg=0.36 fingerprint over 1873 frames: `drop_glow 0.74 + energy 0.71 + centroid 0.90 + entropy 0.84 + spread 0.91 + rolloff 0.94 + treb 0.56 + bass 0.09 (LOW) + bassZ -0.23`. Definitively NOT bass-driven. Cross-referenced against shader audio paths — only **NEBULA FOG** matches all of (drop_glow elevated, entropy/treble high, exterior-circular, additive composite). The block was `col += nebula_col * fog * gate * 0.20 * (1-silhouette)` where `gate = smoothstep(treb)*smoothstep(entropy)` — fires precisely on bright/chaotic passages. Radial sin() field tinted toward CORONA_HUE-0.3 (~3.7 rad = purple-magenta) brightening the whole exterior reads as "circular flash radiating out from behind the taco." **REMOVED.** Concurrent fixes: (1) chrome rim hue was `CORE_HUE + chrome_angle*0.25 + iTime*0.08 + pitchClassN*0.4` cycling through full circle every ~78s — guaranteed magenta moments. Replaced with bounded breath `mix(CORE_HUE, CORONA_HUE+TAU, 0.4Hz-breath) % TAU` — short-arc through indigo, NEVER through magenta. Chroma 0.18→0.13. rim_boost max 4.4→1.4. (2) CORE_HUE / CORONA_HUE locked to plasma-event-horizon/4 anchors (orange family + deep blue-violet family). seed contribution 0.15→0.05. **Patterns recorded:** *Pixel-sampling + audio-feature snapshot is the right methodology for "flash sometimes" hunts* — after 4 prior false fixes (heart pulse, photonRing, coreGlow, god_rays), the hunter found it in ONE session. *Additive radial-sin fields × (1-silhouette) gated on bright/chaotic features = circular flash — permanent rejection.* *Hue rotation cycling via `+ iTime*N` at any rate WILL eventually pass through magenta — for "no magenta" requirement, must use bounded short-arc mix, not continuous rotation.*
- **Iter 44 (2026-05-09) — *ONLYFORYOU. / Holmesick* — FOUND THE CIRCULAR FLASH: god_rays!** — User asked "Is it spectralRolloff?" → sampled all features, rolloff was steady (range 0.275). NOT rolloff. Then grep'd for remaining circular terms tied to drop_glow/IS_DROP — **found leftover GOD RAYS block at lines 366-373** that I never removed. `god_rays = pow(cos(angle*7), 14) * exp(-r*1.5) * IS_DROP` — 7-beam center-radial fan gated by IS_DROP (which fires on every drop AND on rising energy via BUILD term). Composited at line 481 `col += god_rays * ray_col * silhouette * 0.8`. The rays radiated from taco_center on every drop — exactly the "flash radiating out from behind the taco in a circle" user kept seeing. **Fix:** zeroed `god_rays = 0.0` and `ray_col = vec3(0.0)`. **Pattern recorded:** when chasing a "circular flash" bug, grep ALL of: `cos(angle * N)`, `pow(cos`, `exp(-r`, `IS_DROP`, `drop_glow`, `bass_smooth` together. Don't trust earlier "fixes" — multiple sources can each contribute.
- **Iter 43 (2026-05-09) — *Perfect - Of The Trees Remix / CloZee* — REMOVED circular bass-flash from photon ring + core glow** — User: "I am still seeing that flash occasionally! radiating out from behind the taco in a circle." **Debug:** found two coupled circular effects pulsing on bass:
  (1) `photonRing = exp(-pow((r_horizon - photon_ring_r)*6.0, 2.0)) * (0.6 + bass_smooth*0.5 + DROP_FLARE)` — sharp circular ring at knob_12-controlled radius that BRIGHTENED on every bass kick + drop_glow latch.
  (2) `coreGlow = smoothstep(0.5, 0.0, r_horizon) * HORIZON_POWER` where `HORIZON_POWER = 1.2 + bass_smooth*1.0 + drop_glow*0.6` — center glow that ALSO pulsed in sync.
  Combined: bright circular flash radiating from taco_center on every bass kick. **Fix:** stripped bass/drop_glow contribution from BOTH. `coreGlow * 1.2` (constant), `photonRing * 0.6` (constant), removed bassZScore radius push from `photon_ring_r`. The event horizon is now stable structural texture, NOT a beat-driven flash. **Pattern recorded:** ANY bright `exp(-r^2)` or `smoothstep(0, r)` term multiplied by `bass_smooth + drop_glow` produces a circular bass-flash. Don't pulse circular intensities on the controller's bass signals.
- **Iter 42 (2026-05-09) — *Perfect - Of The Trees Remix / CloZee* — Shiver gate retuned with sampled dominant features** — User: "You need to look at whatever audio feature is dominant in that section. Possible spectralRoughness" → "like right now". **Sampled all 14 audio features at the user-marked moment** and ranked by Normalized magnitude. Top 8: entropy 0.96, **spectralSpread 0.89** (HIGHER than user's guess), **spectralRolloff 0.87**, centroid 0.70, treble 0.61, pitch 0.57, roughness 0.52, mids 0.36. **Move:** rewrote shiver gate from `entropy * treble * roughness` to `entropy * spectralSpread * spectralRolloff`. Old gate computed ~0.03 at the marked moment (barely passing 0.02 trigger). New gate computes ~0.5+ (5-10× stronger). User's roughness intuition was directionally right but spread/rolloff are the actually-dominant features. **Pattern recorded:** when user says "look at the audio feature," always sample ALL features and rank — don't trust intuitions about "high treble" vs "high entropy." `spectralSpread + spectralRolloff` are excellent proxies for "wide bright wash texture."
- **Iter 41 (2026-05-09) — *Perfect - Of The Trees Remix / CloZee* — SHIVER on chaotic-bright-rough corner** — User: "I want shivering when the music has this audio signature" → "Now is where I meant. I rewound so you can see it." **Audio fingerprint captured at user's marked moment:** `entropy 0.97 + treble 0.65 + centroid 0.91 + roughness 0.80 + bass 0.13 + mids 0.09`. Pure chaotic-bright-rough texture. **Move:** added a SHIVER effect — high-freq per-pixel hash drives sub-pixel uv jitter (max ~1.5px) on a fresh ink-mask sample. The taco's ink lines TREMBLE on this corner. Gated by `smoothstep(entropy) * smoothstep(treble) * smoothstep(roughness)` — fires only when ALL THREE thresholds hit. NOT a flash, NOT a hue rotation, NOT additive overlay. Pure positional jitter of the existing ink, multiplied into a darkening blend so it just adds extra flickering edge details. Banding-impossible: `mix(col, col * 0.5, ...)` cannot brighten or shift hue.
- **Iter 40 (2026-05-09) — *Perfect - Of The Trees Remix / CloZee* — REMOVED green-bg-flash + sigil swirl** — User: "You need to remove that green flashing from the background though. It's controlled by an audio feature and/or a knob" → "I do not like This spinning, multicolored thing we have right now" → "We need to back out of that. I've seen variations of this effect a lot. Remember I don't like it" → confirmed "Much better" after removal. **Two removals:**
  (1) **GOD RAYS removed** (just-added iter-39 effect that flashed on bass_smooth + drop_glow → green CORONA pulse on every kick)
  (2) **SIGIL SWIRL removed** (the angular spiral with hsl2rgb hue cycle on the coat surface — the actual "spinning multicolored thing"). Replaced both with explicit rejection comments in the .frag so future ports won't re-add. **Pattern locked into rejection list:** angular spirals + continuous hue rotation are PERMANENTLY rejected for this shader.
- **Iter 39 (2026-05-09) — *Perfect - Of The Trees Remix / CloZee* — PLASMA GOD RAYS from silhouette** — User stepped away with directive: "radiating effects like the plasma series, radiating in musically reactive, evolving ways." **Move:** added volumetric god-ray fan that emanates FROM the silhouette outward (logo-aware, NOT center-radial). Each exterior pixel computes proximity to the silhouette via getEdgeGlow at 6-10% radius, then casts beams using `pow(cos(angle * 11-17), 6)` modulation. Beam count varies with `spectralCentroid` (11→17 beams as track gets brighter). Ray rotation phase pulled by `iTime + bass_smooth + pitchClass`. Distance falloff `exp(-r * 1.2)` so beams fade with travel. Audio gate combines `drop_glow + treble + bass_smooth`. Color in oklch CORONA family with ±20° pitch drift (no rainbow possible). **Banding-safe:** additive blend with bounded gate values. **Pattern recorded:** "plasma-style radiating" = volumetric beams using ATAN angular modulation + EDGE-PROXIMITY mask (not radial-from-center) + audio-reactive count/phase. The logo's outline is the source of the energy, not its center.

## Todo
- `[ ] knob_15 currently dead (drip disabled). Re-wire to a different effect — candidate: god rays intensity or aurora veil strength.`
- `[ ] iter 41+: user said "watch me turning the dial" and was turning knob_10 (LENS_STRENGTH range 0.55→0.79). Either showing me what the knob does or wanting it rewired. Wait for next message before changing.`
- `[ ] CIRCULAR FLASH STILL HAPPENING (iter 43+) — user: "I am still seeing that flash occasionally! radiating out from behind the taco in a circle" → after iter-43 fix on photonRing+coreGlow → "Now it's happening" → "we missed it". Iter-43 fix wasn't enough. Remaining suspects to audit: (1) WARM HEARTH `exp(-hearth_d * 8.0)` — corner-gated by warm-bass passages, COULD be the circular bg glow; (2) GOD RAYS `god_rays * ray_col * GODRAY_INTENSITY` — fan beams gated by IS_DROP — confined to silhouette so probably not; (3) The PRISM/edge glow at high knob_4 ramping with drop_glow could read as a bg circle if the silhouette is small in frame; (4) The HEART PULSE was already removed; (5) the chrome rim's bass_smooth*1.2 boost still pulses and could ring around the silhouette edge as a circle. Next time the user catches it, sample the audio + drop_glow + which knobs they're touching to nail the exact firing condition.`
