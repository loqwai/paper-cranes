# clit/2 — Session Journal

## Status
Iter 27 of /vibej run on `shaders/clit/2.frag`. **Shader-swapped from plasma-event-horizon/4 → clit/2** at user request: "Use the others in that series for examples". Live laptop mic input. The clit/2 was previously pure audio-reactive (no knob_* references) — this run is wiring it up.

## What clit/2 is
Chromadepth fractal "body" shader (Dom Mandy complex-power fractal base). Red = closest (focal point), blue/violet = farthest (background). Drop detection via `energySlope * energyRSquared` triggers spotlight effect. Lace-pattern from orbit traps as the only "lit" content; background stays deep velvety purple. Rim lighting on body-silhouette edges, warmth modulated by `spectralRoughnessNormalized`. Beat flash + bass pulse + frame feedback.

**Visual at swap moment:** butterfly/winged silhouette with intricate pearl-pink lace filigree, hot red focal point glowing on the right side, against deep velvety black. Audio in calm sub-bass-only pocket (`bass 0.41 + mids 0.05 + treble 0 + energy 0.02`).

## Key lessons carried forward (from clit/3 sibling)
- **Focal point disguise**: `clit/3` deliberately HID the focal hot-spot — user wanted "audience to not notice it's the same shader until they see it". Different aesthetic intent for /2 vs /3 (/3 is the disguised version).
- **Wing-traveler hearts** are a /3 motif — don't reintroduce here, that's /3's territory.
- **Midnight-blue bg** is a /3 pivot — clit/2 keeps the original deep velvety purple `vec3(0.04, 0.015, 0.08)`.
- **Lace + rim are the only "lit" content** — preserve this composition rule.
- **Heart-pulse / heartbeat / heart embers** were all explored in /3 — don't redo here.
- **Travel speed scales with energyNormalized + bassNormalized** (with floor 0.04) so calm passages breathe slowly — pattern usable on any traveler-style addition.
- The shader uses `time` not `iTime` (handled via shader-wrapper).

## Forks
- `clit/2 ← <vibej swap from plasma-event-horizon/4>` (iter 27, 2026-05-02): tail end of *PressureGENESI – Laherte* session. Previous run was on event-horizon. /vibej state continues with same job ID.

## Cool moments
(pending — capturing live)

## Todo
- `[ ] No knobs were wired before this run. Build up a control bank as user gestures arrive.`
- `[ ] Drop detection is via energySlope * energyRSquared — observe whether real drops fire visibly.`
- `[ ] If user wants to disguise focal further, see /3's iter-7 disguise-pass for the recipe.`
- `[ ] Focal point sits at vec2(0.0, 0.12) — that's the body-anatomy spot. If user wants the focal to MOVE, this is the parameter to vary.`

## History of changes
- **Iter 47 of clit/7 (2026-05-02) — Fangs lunge on bass kicks:** Audio: `bass 0.14 + treble 0.50 + trebZ 1.00 (MAX) + mids 0.86 + entropy 0.44 + energy 0.02 + centroid 0.52`. Treble-z at maximum — the calmed shader handles it without strobing. **Visual:** vampire fangs + drips dominate the dark frame; user's deep-zoom + low POWER_BIAS settings have minimized fractal detail, making the screen-space horror overlays the main motif. **Move:** added `bass_lunge = smoothstep(0.0, 0.6, max(bassZScore, 0))` to tooth_y_extent — kicks extend each fang by up to 25% deeper for a moment. Bass-gated so cymbals/treble don't trigger the lunge. **Pattern recorded:** *for menacing motifs (fangs, claws, weapons), tie depth/aggression to BASS rather than treble.* Bass = body, low-end = threat. Treble = sparkle, high-end = brightness/sharpness. Don't make a fang lunge on a cymbal — it should drive deeper on each kick. **Lesson:** the audio→visual mapping should respect the cultural connotation of each frequency band.

- **Iter 46 of clit/7 (2026-05-02) — VAMPIRE FANGS + DRIPPING BLOOD (user: "dripping blood... sharp teeth down the fractal"):** Two new motifs added on top of iter-45's vampire-rave palette pivot:
  1. **FANGS along the top edge**: triangular tooth profile via `pow(abs(sin(x_screen * 7 * π)), 5.0)` — sharpness from high power, 7 teeth across the screen. Each tooth bites down to depth `0.20 * profile`. Smooth-stepped mask gives crisp edges. Fang color: pearl-white `vec3(0.92, 0.85, 0.88)` with hot crimson **gum-line stain** at the tip (the gum is bleeding). Tooth count subtly drifts with `sin(time * 0.3)` so the bite shifts.
  2. **DRIPPING BLOOD**: 6 vertical drip columns at hash-randomized x positions. Each drip has an independent period (4-9 seconds via second-hash on index), so they don't reset together. Each drip extends from y=0 down to `phase * 0.85`, with a brighter rounded **drop head** at the bottom (length-of-(dx*1.5, dy) circular shape). Pure crimson `vec3(0.85, 0.05, 0.10)` blended over col at 85% opacity.
  **Combined visual:** vampire's teeth biting down from the sky, blood streams falling through the disk in random columns, fractal gore beneath. **Pattern recorded:** for "vampire / bite / drip" aesthetics, two motifs do most of the work — angular teeth (high-power abs-sin) along an edge + vertical hash-positioned drips with independent periods. Both run in screen-space (independent of fractal coords) so they read as overlay/foreground rather than fractal detail. **Lesson:** screen-space overlays free you from the fractal's coordinate system — drips and fangs would be complicated to render in fractal-space, but trivial in screen-space, and they read as "in front of" the fractal which is what blood/teeth visually want to do.

- **Iter 45 of clit/6 → clit/7 (2026-05-02) — VAMPIRE-RAVE PALETTE PIVOT (user: "dark vampire rave vibe. Like blade"):** Forked clit/6 → clit/7. Aesthetic pivot:
  - bg_purple `(0.04, 0.015, 0.08)` → `(0.015, 0.005, 0.025)` (almost pitch-black with subtle blue undertone)
  - rim_cool violet → electric ice-blue `(0.10, 0.45, 0.80)`
  - rim_warm pink → saturated blood crimson `(0.95, 0.05, 0.10)`
  - oceanCast cyan-teal → blood-red `(0.85, 0.05, 0.08)` (WATER_TINT k9 repurposed as BLOOD_DRENCH)
  - caustic color `(0.6, 0.85, 1.0)` → electric blue `(0.15, 0.40, 0.95)` (cold strobe-light shafts)
  - **BLOOD_STROBE** added: `smoothstep(0.4, 0.9, max(bassZScore, 0))` ramps over Z range — cymbals don't pop, only sustained bass kicks (Z>0.5) trigger crimson tint flash + brightness bump. Multiplicative: 1.0 + 0.35 brightness, mix toward `(1.4, 0.4, 0.5)` at 40%.
  **Pattern recorded:** for aesthetic pivots referencing a specific film/genre ("Blade"), the SHADER FORK is the right move — bake the new palette + key new motifs into a new file, leaving the prior fork as the alternate reference. /6 stays as the dwell composition; /7 owns the vampire-rave aesthetic.

- **Iter 44 of clit/6 (2026-05-02) — MONSTER_BASS dims caustics (compounding gate effect):** Audio: `bass 0.71 + bassZ 0.61 + mids 0.10 + treble 0.0 + entropy 0.11 + flux -0.21 + energy 0.0 + centroid 0.13`. Sub-bass-only pocket continues — MONSTER_BASS gate firing at ~0.30 (partial). **Visual:** rose-pink/maroon dreamspace with spherical "moon" floating right-of-center. **Move:** added `* (1.0 - MONSTER_BASS * 0.6)` to CAUSTIC_INTENSITY. Caustics now dim 40% during full monster pockets. Reasoning: caustics represent sunlight refraction through water — in the lurking-monster regime (deep, dark, bass-only) we're "too deep" for sunlight. **Pattern recorded:** *gates compound* — once a feature-corner gate exists, multiple visual responses can hook into it for compounding effect. MONSTER_BASS now does TWO things: deepens DEPTH_FOG (iter 43) AND dims caustics (iter 44). Each gate becomes a visual register where multiple effects converge on a shared mood. **Lesson:** gate-driven design > per-feature wiring. Build feature-corner gates first, then stack effects on them.

- **Iter 43 of clit/6 (2026-05-02) — MONSTER_BASS gate ported from plasma-event-horizon:** Audio: `bass 0.85 + bassZ 0.62 + mids 0.15 + treble 0.0 + entropy 0.16 + energy 0.23 + centroid 0.05`. Textbook sub-bass-only/dr-fresch lurking pocket. **No equivalent gate existed in clit/6** despite the plasma sessions documenting this as a recurring track-character. Ported the MONSTER_BASS gate concept: `smoothstep(0.5, 0.8, bassNormalized) * smoothstep(0.6, 0.2, midsNormalized) * smoothstep(0.4, 0.0, trebleNormalized) * smoothstep(0.3, 0.0, spectralCentroidNormalized)`. Fires only on this specific four-feature corner. Used to **deepen DEPTH_FOG** by adding `+ MONSTER_BASS * 0.35` (clamped) — sub-bass pockets push the void inward (lurking-monster compression). Subtle but specific. **Pattern recorded:** feature-corner gates are a portable DSL across shaders. The same gate concept worked on plasma-event-horizon and now clit/6 — same audio territory, different shaders, same aesthetic territory it names. **Lesson:** when a shader transitions to a track-character that prior shaders had a gate for, port the gate. The gates form a library of musical-territory detectors.

- **Iter 42 of clit/6 (2026-05-02) — RIM_INTENSITY Median+Normalized blend:** Audio: `bass 0.24 + bassZ -0.49 + treble 0.29 + trebZ 0.78 (rising hard) + mids 0.75 + entropy 0.77 + flux 0.28 + energy 0.49 + centroid 0.67 (bright!) + rough 0.69`. Track shifted to a brighter, treble-entering passage. **Diagnosis:** the iter-36 fix (`trebleNormalized → trebleMedian * 0.5`) cured cymbal strobing but made rim too laggy — Median takes minutes to catch up to true treble shifts. With trebZ 0.78 rising NOW, the rim isn't responding. **Move:** `RIM_INTENSITY = 0.4 + trebleMedian * 0.4 + trebleNormalized * 0.15`. Median provides 80% of the prior coefficient as stable baseline (no strobe), Normalized adds modest current-level tracking (no Z-score, smoother). Total max ~0.55 (slightly above prior 0.5). Won't strobe on cymbal hits, will respond to sustained treble passages. **Pattern recorded:** post-strobe-fixes, sometimes the over-correction is noticeable. The fix is BLEND: `Median * primary_coef + Normalized * minor_coef` — most weight on Median for stability, small Normalized weight for responsiveness. Z-score still avoided in iteration-control parameters. **Lesson reinforced:** the post-pullback rule of thumb extended: *Median for iteration-control, Median+Normalized blend for color/intensity that should respond to character shifts, Normalized only for additive overlays, Z-score only for transient impacts.*

- **Iter 40 of clit/6 (2026-05-02) — caustic intensity tracks centroidNormalized (current brightness):** Audio: `bass 0.28 + mids 0.72 + entropy 0.70 + energy 0.65 + energyZ 0.37 + centroid 0.62 + rough 0.71`. Track shifted to a brighter, more open section — centroid jumped from ~0.10-0.20 range to 0.62. **Diagnosis:** the fractal POWER `A` uses `spectralCentroidMedian` (smooth, calibrated lag) so the iteration doesn't catch up immediately to bright/dark transitions. **Move:** added `(0.5 + spectralCentroidNormalized * 0.7)` multiplier to CAUSTIC_INTENSITY. Caustics now scale with CURRENT brightness — brighter passages reveal more sunlight-spot caustics, darker passages dim them. **Why safe:** caustics are an additive overlay (not in iteration math), and `Normalized` is the smoothed current level (not the spiky Z-score). No strobe risk. **Pattern recorded:** "Median for iteration, Normalized for additive overlays" is the post-pullback rule of thumb. Median is for things that need iteration stability (won't strobe even if Z-score would); Normalized is for things that should track current level smoothly (additive layers, color modulation, brightness floor multipliers); Z-score is for transient impacts only (drops, drop flares, beat punches).

- **Iter 39 of clit/6 (2026-05-02) — focal_center.y drifts with midsMedian:** Audio: `bass 0.10 + bassZ -0.30 + mids 0.88 + midsZ 0.76 + treble 0.0 + entropy 0.35 + energy 0 + centroid 0.16 + pitchZ 0.47`. Mid-dominant moment (vocal/lead zone). **Visual:** stunning pink/magenta/yellow flame fractals flowing toward vanishing point lower-right with deep filigree detail (k13 ITER_DEPTH at MAX, k15 FOCAL_GAIN at MAX). User dwelling on this comp. **Move:** added `+ (midsMedian - 0.5) * 0.1` to focal_center.y so vocal-zone passages float the focal slightly higher, dropping it on bass-only sections. **Median used deliberately** — no z-score, no strobe risk. ±0.05 maximum drift. Subtle modulation of focal POSITION (not intensity) — mids gets a smooth visual signature without flashing. **Pattern recorded:** post-pullback, the safest way to add audio-reactivity is *position drift via Median*. Median variations are slow enough that even POSITION drift (which would strobe with Z-scores) reads as gentle motion.

- **Iter 38 of clit/4 (2026-05-02) — REACTIVITY PULL-BACK (user: "It's way too flashy and reactive, we have to pull it back"):** Audited every audio→visual coefficient and cut hard across the board. Ten changes:
  1. `PULSE 0.06 → 0.02` + clamp `bassZScore` to positive (was darkening on negative bass-Z)
  2. `FEEDBACK_MIX 0.1 → 0.04` + use `energyMedian` not `energyNormalized`
  3. `RIM_WARMTH 0.4 → 0.25` + `spectralRoughnessNormalized → spectralRoughnessMedian`
  4. `AUDIO_ZOOM` all coefficients halved (bassZ 0.08→0.03, BUILD 0.18→0.08, DROP 0.06→0.03)
  5. `TUBE_FALL_SPEED` audio additions cut: was `+ energyNormalized*0.6 + bassZ*0.3`, now `+ energyMedian*0.2` only — fall is much steadier
  6. `flux_shimmer 0.4 → 0.12`
  7. Autonomous time-modulators in iteration loop reduced: `timePower 0.08→0.025`, `timeBody 0.06→0.02`
  8. theta wobble: `T1*0.12 + T3*0.06` → `T1*0.04 + T3*0.02`
  9. curl drift: `T2*0.15 → T2*0.05`
  **Pattern recorded:** when a user reports "too reactive," don't try to find the ONE culprit — audit ALL audio→visual paths and halve coefficients across the board. The cumulative effect of many small modulations is what creates "flashy" feel. Single-source fixes won't catch interaction effects. Then: replace ZScore/Normalized with Median where the parameter is being USED FOR DRIFT (not for transient impacts). Z-scores belong in things that should pop on transients (drop flares, beat punches); Median belongs in things that should drift slowly. **Lesson: the line between "responsive" and "twitchy" is set by which statistical variation you reach for.**

- **Iter 37 of clit/4 (2026-05-02) — FRAME-BLACKOUT FIX (user: "entire frame blacking out every other frame"):** Iter 36's removal of entropy from WALL_COMPLEXITY and trebleNormalized→Median in RIM_INTENSITY didn't fix the user's actual complaint — they were seeing whole-frame blackouts (not flashes), which is a different symptom requiring a different diagnosis. **Real culprit:** `#define A mapValue(spectralCentroidZScore, ...)`. The fractal POWER A is used in `pow(d, V.x/2.)` inside the iteration. When centroid Z-score spikes from cymbal hits, A jumps between frames → `pow()` returns wildly different values → fractal iteration explodes/collapses → the whole frame's lace+rim+focal all collapse to ~0 → `bright_allowed` global multiplier kicks in (`col *= mix(0.15, 1.0, 0)` = 0.15× black) → whole frame goes near-black. **Fix:** `A` now uses `spectralCentroidMedian` (slow baseline). Also fixed `B` similarly: `energyZScore` → `energyNormalized` for smoother drift. **Critical lesson recorded:** *audio inputs to ITERATION CONTROL parameters (power, body offset, anything inside the recursion) must be stable.* Otherwise frame-to-frame parameter changes desync the iteration's convergence and the whole image flickers. This is BEYOND "audio in phase = strobe" — it's "audio in any value the recursion depends on for stability." Use Median/Mean for these slots; reserve Z-scores for additive amplitude effects only.

- **Iter 36 of clit/4 (2026-05-02) — STROBE FIX (user: "cymbals are making it flash like crazy"):** Diagnosed two strobe sources:
  1. **WALL_COMPLEXITY had `* (1.0 + spectralEntropyNormalized * 0.6)` baked in.** This multiplier was inside `C.x = theta * WALL_COMPLEXITY * 0.18` — meaning entropy scaled the COORDINATE used for fractal sampling. Cymbals = entropy spikes → frame-to-frame coordinate scale jumps → fractal samples wildly different positions → strobe. Same bug class as plasma-event-horizon iter-19's RADIATION_LOUDNESS-in-phase strobe. Removed the entropy multiplier — WALL_COMPLEXITY is now pure knob.
  2. **RIM_INTENSITY = `0.4 + trebleNormalized * 0.6`** — `trebleNormalized` spikes hard on cymbals. Replaced with `trebleMedian * 0.5` (slow-drifting baseline) — rim now brightens with sustained treble character, not on every transient hit. Coefficient reduced to compensate for median's narrower range.
  **Lesson reinforced (third time this run, second on this shader):** *audio belongs in amplitude/color, never in phase/position*. When users report flashing/strobing, audit the SHADER for any audio feature appearing inside coordinate computations or trig phase arguments. Move them to amplitude multipliers or use slow-statistical variants (Median/Mean) instead of Normalized/ZScore.

- **Iter 35 of clit/4 (2026-05-02) — Audio-driven tube fall speed:** Audio: `bass 0.38 + mids 0.62 + entropy 0.76 + energy 0.43 + energyZ 0.17`. Mid-bass with high entropy — texture-rich. Visual showing dramatic asymmetric chasm composition (top half: pearl/cyan/pink fractal cathedral cascading down; bottom half: void). User k14=DESCEND_Y at 0.96 created the split. **Observation:** user has barely touched the new tube knobs (k19, k22, k23 still at or near 0). The whole "falling down the tube" feel is dampened because k19 TUBE_FALL_SPEED is at minimum (k19=0 → 0.05). **Move:** added `+ energyNormalized * 0.6 + max(bassZScore, 0) * 0.3` to TUBE_FALL_SPEED so loud/kicking passages accelerate the fall regardless of knob position. Now even with k19=0, current audio (energy 0.43) adds ~0.26 to fall speed (a 5× boost). User still controls baseline; audio adds dynamic acceleration. **Pattern recorded:** when adding a major aesthetic feature (tube fall) and user doesn't immediately engage its dedicated knob, give the feature an AUDIO BACKDOOR so it works expressively without manual intervention. Knob = baseline + character; audio = drive + intensity. Both active means the feature is alive even when user is composing other knobs.

- **Iter 34 of clit/4 (2026-05-02) — TUBE-FALL TRANSFORM + multi-frequency time motion (user: "zooming down this tube as a result of time" + "more things going on with time"):** Major aesthetic pivot from "flying through fractal" to "falling down a tunnel." Three concurrent changes:
  1. **Polar tube transform**: replaced cartesian `C` setup with `r = length(uv)`, `theta = atan(uv.y, uv.x)`, then `tube_position = time * TUBE_FALL_SPEED + 0.5/(r+0.15)`. Wrapped via `mod(tube_position, 6.28)` so fractal coords stay in fertile range. Pixels near center = far ahead (haven't reached yet), edges = close (just passed). Time advances tube_position monotonically → walls scroll TOWARD us → permanent "falling forward" feel. **Initial attempt left screen black** (1/r blew up coordinates); fix: clamped `r+0.15` denominator + `mod(tube_position, TAU)` wrap + reduced C scaling factors.
  2. **Tube knobs (k19, k21, k22, k23)**: TUBE_FALL_SPEED, WALL_COMPLEXITY (× spectralEntropyNormalized for live audio drive), WALL_TWIST (helical descent), DEPTH_FOG (far-darkening for depth perception).
  3. **Five coprime sub-Hz time oscillators** added globally (T1=sin(0.13Hz), T2=cos(0.07Hz), T3=sin(0.21Hz), T4=cos(0.05Hz), T5=sin(0.31Hz)). Used for: theta wobble, C.x/C.y drift, water ripple wander, **time-modulated CURL_SPIRAL** (the iteration rotation matrix slowly drifts), **timePower** added to fractal A coefficient (the structure morphs as we fall), **timeBody** added to the body offset (the fractal's anchor wanders). Net: even with all knobs static, the shader now has rich autonomous motion at multiple frequencies.
  **Result reads as:** falling down a curved tunnel with cyan-pink walls, fractal filigree clinging to the surfaces, dark vanishing point dead-center, far-detail emerging as we approach. **Pattern recorded:** "time everywhere" means time as multiplier on slow oscillators that drift each parameter slightly — not just one global time advance. The shader has more places audio/time can show up than a single coordinate transform implies. **Lesson reinforced:** when changing the coordinate system (cartesian → polar), the fractal iteration's expected coordinate range changes; reduce scale factors AND wrap unbounded inputs.

- **Iter 32 of clit/2 (2026-05-02) — k1+k2 → CAM_X/CAM_Y (user: "I need knob_1 and knob_2 control the x and y"):** User explicitly asked for k1/k2 to be camera pan controls. Repurposed both:
  - **k1 → CAM_X** = `mix(-0.6, 0.6, knob_1)` — horizontal camera pan
  - **k2 → CAM_Y** = `mix(0.6, -0.6, knob_2)` — vertical camera pan
  - **FOCAL_GAIN relocated to k15** (was on k1)
  - **CURL_SPIRAL relocated to k17** (was on k2)
  Applied as `C.x += CAM_X; C.y += CAM_Y` after zoom transform. DESCEND_Y still adds on top of CAM_Y for compounding "looking down deeper" feel. **Pattern recorded:** when user explicitly requests specific knob bindings, reassign and relocate the previous functions to other free slots — preserve the features, redirect the controls. The knob bank is the user's contract; their requests trump auto-wire intuitions.

- **Iter 31 of clit/2 (2026-05-02) — k16 → DESCEND_SPEED (auto-wire):** Audio: mid-bass with falling energy. User got lost in murk after deep zoom + low ITER_DEPTH + max RIPPLE settled into washed-out fog. Wired k16 to DESCEND_SPEED multiplier (was hardcoded). Gives them control over fall rate.

- **Iter 30 of clit/2 (2026-05-02) — DEPTH DESCENT BANK + k11 wiring (user request: "I want to go down!"):** User wanted knobs to "zoom in and down the depths of this shader's fractal". **Five additions in one tick:**
  1. **k11 → RIPPLE_GAIN** (auto-wire, was the biggest unwired wiggle): water ripple amplitude `mix(0, 0.05, knob_11)` — 0=steady, 1=wildly distorted turbulent water
  2. **k12 → DEEP_ZOOM**: multiplicative on top of k7 USER_ZOOM with much wider range `mix(0.5, 4.0, knob_12)`. Combined with k7 max gives 8.8× total zoom — true plunge into the fractal.
  3. **k13 → ITER_DEPTH**: caps the fractal iteration count `mix(10, 50, knob_13)` via early-break in the loop. Lower = shallow rendering, higher = full 50 iterations of fine detail. Lets user trade detail for performance OR get less-detailed "broad" structure.
  4. **k14 → DESCEND_Y**: vertical view shift through fractal `mix(0.6, -0.6, knob_14)`. Literally moves the camera DOWN in fractal-space — looking down into deeper parts of the structure.
  5. **AUTO_DESCEND**: `sin(0.05Hz) * 0.15 + time * 0.008` slow continuous downward drift, scaled into C.y. Even with knobs static, we're slowly falling through the fractal — the descent never stops.
  **Pattern recorded:** when user says "deeper" / "down", give them MULTIPLE axes of descent (zoom magnification + iteration depth + vertical shift + auto-drift) so they can compose the fall — not just one zoom slider. Fractals have many axes of "deeper," each with a different visual signature; expose them separately.

- **Iter 29 of clit/2 (2026-05-02) — Drop-anticipation zoom (build → pull back):** Audio: `bass 0.21 + mids 0.77 + treble 0.0 + entropy 0.63 + flux 0.26 + energy 0.18 + energyZ 0.63 (RISING HARD) + centroid 0.19 + pitchZ 0.59`. **Mid-dominant rising-energy buildup** with significant pitch event. The shader has `BUILD_INTENSITY` defined (uses energySlope * energyRSquared) but it was only used for a subtle depth compress. **Visual at edit:** bioluminescent jellyfish-tendril lace at corners with red caustic blobs floating in dark middle. User exploring k5 LACE_THICKNESS to ~0 (hairline) and k8 FOCAL_X to right edge. **Move:** added `+ BUILD_INTENSITY * 0.18 - DROP_INTENSITY * 0.06` to AUDIO_ZOOM. Builds now zoom OUT (camera pulls back for impact), drops zoom IN (commits to focus). Single-line addition leveraging the shader's existing build/drop detection. Subtle but musically-aware. **Pattern recorded:** when a shader has audio-detection machinery that's underutilized (BUILD_INTENSITY only doing subtle depth-mix), wire it into a SECOND visual axis for compounding effect. Each "drop event" should fire at least 2 visual responses for the moment to register.

## Cool moments

### iter 29 — bioluminescent jellyfish tendrils
**Audio fingerprint:** `mids 0.77 + entropy 0.63 + energyZ 0.63 RISING + pitch event`. Building toward a drop.
**What worked:** k5 hairline lace + k8 right-edge focal + caustics (k10) + water tint produces a deep-ocean-bioluminescence aesthetic with red blob caustics floating in the dark middle. Very different from any previous mode. Underwater pivot landing distinctively.
**Design hypothesis:** the lace control + focal drift gives users a fast lever to compose asymmetric scenes (which the original audio-only shader couldn't make). Per-knob composition is a real new affordance.

- **Iter 28 of clit/2 (2026-05-02) — RIBBON CONTROL BANK + UNDERWATER VIBES + ZOOM (user request):** User: "I need the 'ribbons' curling complexity driven by knobs. Give me a ton of continuous shaders for exploring this complex fractal. And vibes, like we're underwater. Have it zoom". **Massive single-tick wiring** delivering all three asks at once:
  1. **Ribbon-control knobs (k2-k8)** all centered on 0.5 = baseline:
     - `k2 → CURL_SPIRAL` — per-iteration pre-rotation of V (twists ribbon spiral, range -0.45..+0.45 rad)
     - `k3 → POWER_BIAS` — additive offset on fractal power A (looser/tighter folds)
     - `k4 → BODY_BIAS` — shifts iteration anchor offset (translates body in fractal-space)
     - `k5 → LACE_THICKNESS` — slides smoothstep range so lace lines thicken/thin
     - `k6 → LACE_SHARPNESS` — pow exponent 1.5..6.0 (soft glow → binary edges)
     - `k7 → USER_ZOOM` — 0.45..2.2× zoom into the fractal
     - `k8 → FOCAL_X` — drifts focal-trap horizontally for exploration
  2. **Zoom motion**: `AUTO_ZOOM = 1.0 + sin(0.21Hz)*0.18 + sin(0.07Hz)*0.10` — slow continuous zoom alive even with knobs static. `AUDIO_ZOOM = 1.0 - bassZ * 0.08` — bass kicks add subtle zoom punch.
  3. **Underwater vibes**:
     - `WATER_RIPPLE` — sub-Hz UV displacement (sin/cos at coprime frequencies on x and y) — wavering as if seen through moving water
     - `k9 → WATER_TINT` — cyan/aqua palette cast (multiplies col toward `vec3(0.05, 0.45, 0.55)` ocean-cast)
     - `k10 → CAUSTIC_INTENSITY` — bright slow-drifting sunlight refraction spots, two layers of sin/cos at coprime frequencies create dappled-light pattern
  4. **Pre-iteration coordinate transform**: combined zoom = `USER_ZOOM * AUTO_ZOOM * AUDIO_ZOOM`, applied to C before V iteration. Water ripple added to V right after.
  **Pattern recorded:** when user asks for "a ton of knobs" plus a wholesale aesthetic shift, deliver multiple wirings AND the aesthetic in one tick rather than dribbling them out — the user wants to start exploring NOW, and the audio/visual feedback loop is the design tool. Knobs centered on baselines means default position keeps current look so they can wiggle to discover the range.

## Cool moments

### iter 41 — *PressureGENESI – Laherte* — VORTEX EYE WITH DEBRIS
**Audio fingerprint:** `bass 0.64 + mids 0.38 + treble 0 + entropy 0.42 + energy 0.44 + centroid 0.48 + rough 0.26`. Mid-bass groove with sustained energy.
**What worked:** User composition: k1=0 (CAM_X left), k7=0.961 USER_ZOOM, k8=0.961 FOCAL_X right, k12=0.339 DEEP_ZOOM moderate, k13=0.819 ITER_DEPTH high, k14=0 looking up, k15=0.898 FOCAL_GAIN high, k16=0.039 near-frozen fall, k5=0.094 hairline lace, k6=0.094 soft glow. Result: concentric purple/black rings winding inward toward hot red eye, asymmetric debris splash lower-right with pink/cyan/yellow fractal filigree, violet streak right edge. Reads as hurricane eye with debris streaming past — the iter-40 caustic-centroid coupling visibly enhanced the bottom-right detail.
**Tick choice:** no edit this tick — user is in active composition mode and the visual is landing. Best service is to not interrupt.
**Design hypothesis:** when the visual is hitting and the user is composing within existing knobs, the right /vibej tick is sometimes no edit. Better to journal the cool moment and let the next tick respond to fresh signals.

### iter 28 — *PressureGENESI – Laherte* — DEEP RIBBONS WITH UNDERWATER POCKETS
**Audio at edit:** `bass 0.41 + mids 0.05 + treble 0 + energy 0.02 + centroid 0.11`. Calm sub-bass-only.
**What worked:** User immediately cranked into deep zoom (k7=0.78) and the ribbons are visibly curling and unfurling. Pink/red dominant with teal underwater pocket bleeding through in the upper-middle. The fractal complexity exploded into a cathedral-of-ribbons reading — far more than the original lace pattern. Knobs 2, 3, 5, 6, 8 all engaged within a minute of wiring. The aesthetic landed instantly.
**Design hypothesis:** wiring 8+ knobs in one tick when the user asks for exploratory control creates a co-designed playspace fast. The shader becomes a collaboration tool the moment the controls are live.

- **Iter 27 of clit/2 (2026-05-02) — INAUGURAL KNOB WIRING (k1 → FOCAL_GAIN):** Audio: `bass 0.41 + bassZ -0.16 + mids 0.05 + treble 0.0 + entropy 0.16 + flux -0.21 + energy 0.02 + centroid 0.11 + pitchZ -0.65`. Calm sub-bass-only. **First-ever knob added to this shader.** Wired `FOCAL_GAIN = knob_1 * 0.6` to the chromadepth focal pop coefficient (was hardcoded 0.3). At k1=0 the focal is invisible (/3-style disguise). At k1=0.5 baseline 0.3. At k1=1 the chromadepth pop blazes to 0.6 (double baseline). Single-line additive change — preserves the existing audio-reactive design, just gives user direct control over the "is the body visible" axis. **Pattern recorded:** when starting work on a shader with no knob wirings, pick the SINGLE most expressive coefficient (one that the user is most likely to want to manipulate live) and wire that first. Build the bank organically from there.

## Design hypotheses for v(next)
- (collecting — need more iters)
